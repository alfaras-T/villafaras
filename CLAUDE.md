# villafaras.jp

日本の高級ヴィラ・グランピング施設のアフィリエイトサイト。GitHub Pages で公開。
姉妹サイトに Alfaras（alfaras.jp）がある。

---

## 技術的な制約（最優先）

iPad Safari を含む古い環境を対象にするため、以下を必ず守る。他環境向けの一般的な
ベストプラクティスより優先する。

- **JavaScript**: ES5 構文のみ。アロー関数、テンプレートリテラル、const/let は使わない
- **非同期通信**: `fetch` ではなく `XHR` の `onreadystatechange` パターン
- **CSS**: 必要に応じて `-webkit-` プレフィックスを付与
- **画像**: base64 埋め込みは使わない。ファイルとして別途保存
- **外部ライブラリ不使用**

`spec.js` 冒頭にもこの方針が記載されている。

---

## リポジトリ構成

```
index.html            # トップページ。VILLAS 配列に全286施設のデータを埋め込み
spec-data.js          # 施設別の詳細スペック（286施設）
spec.js               # スペックのスキーマ定義（55項目）＋ 表示ロジック
reviews.js / .css     # 口コミ機能（Firebase Firestore）
data/desk-research.js # 机上調査の一次記録（監査用）
data/coordinates.json
tools/
  fix_villa.py        # 既存データの訂正
  merge_desk.py       # 机上調査データの反映
  channel_a.py        # 座標からの自動算出
  winter.py           # 標高から winter_access を導出
  extract_b.py        # desc/feature/tags からの自動抽出
  places_geocode.py / merge_spec.py / stats.py / run.py
villas/               # 施設個別ページ
```

---

## データ設計（チャネル A〜D）

| チャネル | 内容 | 項目数 | 状況 |
|---|---|---|---|
| A | 座標からの自動算出（標高・スーパー・コンビニ・IC・駅・温泉） | 6 | 完了 100% |
| A | 標高からの導出（冬季アクセス。`tools/winter.py`） | 1 | 233/286 |
| B | 公式サイト・OTA・サウナイキタイ・楽天の机上調査 | 11 | 進行中 67.2% |
| C | オーナー調査（spec-survey.html） | 34 | 未着手（机上で 14.0% が埋まった） |
| D | 宿泊者レビューの構造化フィールド | 5 | 未着手 |

`spec.js` の各項目には `ch:` で取得チャネルが記録されている。この値は
**2026-08 に机上調査済み57施設での実測充足率をもとに見直し済み**。
設計時の想定と実態が食い違っていた8項目を修正した。

`desk` と書ける条件は「公式サイトに記載される慣習があること」。
記載が慣習化していない項目は充足率が1割を切るため `owner` とする。

---

## 作業ルール

### 訂正は tools/fix_villa.py

`FIXES` に書いて `--dry-run` で確認してから実行する。
リポジトリ直下で実行すること（パスは相対）。

対応する操作: `remove_tags` / `set_spec` / `remove_spec` / `set_villa` /
`old_desc`+`new_desc`

`set_villa` は index.html の VILLAS スカラー項目を訂正する。`ota` の値の
差し替えにも使う（キーの削除は `remove_ota`）。**個別ページ側では ota リンクを
ボタンのラベルで特定している。** URL の文字列置換にすると、旧い値が同じページの
別のURLの前方一致になったり（id=113 は公式リンクが Agoda の URL に書き換わる
ところだった）、2つのキーが同じURLを持つ場合に取り違えたりする（id=246）。

`capacity` / `checkin` / `checkout` は villas/*.html の fact 行にも描画されるので
そちらもあわせて書き換わる。**定員は index.html・個別ページ・spec-data.js の
3箇所に出る。** `set_spec` だけでは片手落ちになる。

**住所は villas/*.html のなかに4通りの形で出る。**

| 形 | 例 | 平文の置換で当たるか |
|---|---|---|
| 本文 | `<div class="modal-addr">住所<a href=…` | ○ |
| JSON-LD | `"streetAddress": "住所"` | ○ |
| 地図リンク | `maps/search/?api=1&query=施設名%20住所` | **×（percent-encode）** |
| 地図埋込 | `maps?q=施設名%20住所&output=embed` | **×（percent-encode）** |

**後ろ2つに当たっていなかったため、住所を訂正した施設ほど地図が誤っていた**
（2026-09 に16施設32URL）。id=239 AMAO VILLA の地図は姉妹施設の住所を、
id=132/133 GEOSPOT の地図は入れ替わった住所を指したままで、住所監査で直した
id=115/118/124/125/189/199 も全滅していた。**本文と JSON-LD だけ新しくなり、
利用者が「大きな地図で見る」を押すと誤った場所に飛ぶ**という、いちばん気づき
にくい壊れ方をする。

`fix_villa.py` に符号化形の置換を入れて再発を止めた。**ただし既に訂正済みの
施設は旧住所がDBに残っていないのでツールでは直せない。** DBの `name` + `addr`
から作り直した。`validate.py` の `check_map_urls` が4箇所すべてを照合する。

### 机上調査の反映は tools/merge_desk.py

```
python3 tools/merge_desk.py out/spec-data-desk.js data/desk-research.js
```

既存値がある項目は上書きせずスキップする（既存優先）。
**`spec.js` の SCHEMA に無い項目も入れない。** 廃止した項目が一次記録に残っていると
マージで黙って復活する。2026-09 に `early_late` を分割して spec-data.js から消したが、
`data/desk-research.js` の古い記録が次のマージで15施設ぶんを復活させた
（validate.py が「SCHEMA にない項目」15件で検出）。弾いた項目は件数と id を出す。

**削除した値も、一次記録に残っていればマージで復活する。** 2026-09 に id=131 の
`kids_free` を削除した直後のマージで実際に戻った。`fix_villa.py` の `remove_spec` が
一次記録からも同じセルを消すようにして解消した。**spec-data.js だけ直しても効かない。**

**候補ファイルの書式でマージが黙って壊れた（2026-09。`parse()` を直して解消）。**
`parse()` が施設ブロックの終端を `\n\s*\}` で探していたため、

```
  "0":  { stove:{...},
          rest_chair:{...} },     ← 閉じ括弧が最終セルと同じ行
```

と書いた候補ファイルで終端が見つからず、**id=0 のブロックがファイル全体を飲み込んだ。**
23施設81セルがすべて id=0 に集まり、同名フィールドは後勝ちで潰れて、
**id=53 の `sauna_temp` と id=56 の `stove` が id=0 に書き込まれた。**

**エラーも警告も出ず「追加 6 フィールド」とだけ表示された。**（81のはずだった）
**件数を見ていなければ通っていた。** マージ後は必ず追加件数が想定と合うか確かめること。
`parse()` は波括弧の対応で切り出すようにし、ブロック内に別の施設idが現れたら停止する。

**この既存優先が、後から調べた正しい値を黙って捨てる。** 見送りは集計件数しか
出ないので気づけない。2026-08 に定員2件（軽井沢365 リバーサイドヴィラ、
トライハク神楽）が、desk-research.js に正しい値がありながら反映されていな
かったことが判明した。マージ前に必ず次を実行して食い違いを確認すること。

```
python3 tools/validate.py data/desk-research.js
```

一次記録として `data/desk-research.js` にも追記する。
**fix_villa.py で訂正した施設も desk-research.js に記録すること。**
記録漏れがあると未調査リストに残り続ける。

### 調査対象の選定は tools/blanks.py

**空欄の数え方とエージェントに渡す選択肢マスタを手で書かない。** 2026-08 に
両方で事故を起こした。

```
python3 tools/blanks.py            上位20施設を一覧
python3 tools/blanks.py -n 14      件数を指定
python3 tools/blanks.py --prompt   調査指示（選択肢マスタ＋対象）を出力
python3 tools/blanks.py --unsourced   出典URLの無い値が多い順（検証の波用）
python3 tools/blanks.py --ids 1,2,3   対象を明示。並行する波と重複させないため
python3 tools/blanks.py --include-done  記録済みでも空欄が残る施設を出す
python3 tools/blanks.py --yield       空欄数ではなく期待収量で並べる
python3 tools/blanks.py --ikitai      サウナイキタイ未確認のサウナ施設に絞る
```

**`--ikitai` は 2026-09 に使い切った（255/255・未走査0件）。** 走査済みの印は
`data/ikitai-checked.json` が持つ。**spec-data.js の出典URLで代用してはいけない**
（掲載が無かった施設には出典が残らないので「調べたが載っていなかった」と
「まだ調べていない」を区別できず、実際に千葉36件・山梨23件を再掲した）。

**後半の波では `--include-done --yield` を使う。** 既定は `data/desk-research.js` に
記録のある施設を除外するが、286施設中279が記録済みになった時点で、既定の一覧は
上位14件で空欄50しか出さなくなった。実際には同じ14件に空欄115が残っていた。
`--yield` は空欄の数ではなく**項目ごとの充足率を重みにした期待収量**で並べる。
空欄数で並べると `coldbath_season`(4%) や `stove`(35%) のような「公式が書かない項目」
ばかりに当たるため。

- **セルの正規表現は空白数を決め打ちしないこと。** spec-data.js は
  `capacity:     { v: 8, ... }` のように `key:` のあとの空白数が揃っていない。
  `(\w+): \{ v:` と書いたため大半のセルを取りこぼし、調査済みの施設が
  「空欄10」に見えて**14施設中12施設を二重調査させた**。id=92 は22項目
  入っているのに1項目しか数えられていなかった。
  総フィールド数を validate.py と突き合わせれば 724 対 3975 で気づけた
- **選択肢マスタは `spec.js` の `var O` から生成する。** 手で写した際に
  `loyly` を yes/no の2択と書いて `auto`（オートロウリュ）を落とし、
  `sauna_type` に存在しない `cabin` を混ぜた

### コミット

小さく、意味のある単位で分ける。メッセージは日本語。
出典となる原文を引用し、除外した項目とその理由も併記する。

---

## 生成データの検証（必須）

**`tools/validate.py` を通す。** 手でやっていた確認をすべてこれにまとめてある。

```
python3 tools/validate.py                       spec-data.js（マージ後込み）
python3 tools/validate.py data/desk-research.js 候補ファイル
python3 tools/validate.py --allow-remove        訂正で項目を消したとき
```

エラーがあれば終了コード 1。選択肢マスタと項目定義は `spec.js` の
`var O` / `var SCHEMA` から直接読むので、スキーマを変えても検証側の修正は要らない。

見ている内容は次の3段階。

1. **選択肢照合** — `spec.js` の `var O = {...}` にある選択肢マスタと突き合わせる
2. **単位確認** — 数値項目は `u:` の単位（℃/名/口/歳まで/円）と意味が一致するか
3. **型チェック** — 選択肢型の項目に数値を入れていないか

過去の失敗例（いずれも validate.py で検出できることを確認済み）:
- `steps` に存在しない値 `'many'`（正しくは `flat` / `stairs`）
- `kids_free`（単位「歳まで」）に人数を入れかけた
- `water_temp`（選択肢型 u10/t1015/t1518/t1822/o22）に数値16を入れかけた
- `firepit` の「不可」を `'none'` と書いた（正しくは `'no'`）

範囲だけでは拾えない単位の取り違えは交差チェックで見ている。

- `comfort_cap > capacity` … 推奨人数が定員を超える（2件見つかった）
- `kids_free == capacity` … 「歳まで」に人数を入れた疑い
- `sauna_exists=no` なのにサウナ項目がある … 訂正時の消し忘れ
- **`late_arrival` と index.html の `checkin` の整合**（2026-09 に追加。4件見つかった）
  … `no` なのに受付が22時以降まで、`ok` なのに21時前に終わる。
  **倒れる向きが施設ごとに違うのでエラーではなく警告**にしてある

**この `late_arrival` の検査は4件すべてで当たり、4件とも中身が違った。**

| 施設 | 誤っていたのは | 訂正 |
|---|---|---|
| id=19 GIFTHOUSE 那古海岸 | `checkin`（姉妹施設の値） | 15:00〜24:00 → **15:00〜18:00** |
| id=43 Montevan | `checkin` | 15:00〜24:00 → **15:00〜20:00** |
| id=269 THE LOOKOUT KUSATSU | `late_arrival`（出典の読み違い） | ok → **no** |
| id=280 藤右衛門 | `late_arrival`（根拠が消滅） | no → **削除** |

**id=269 は見出しだけを読んだ誤り。** 出典 aco.co.jp の同じページにこう書いてある。

  「《チェックイン》 0.**鍵の受け取りは20時30分までに**お願いいたします。
    それ以降は閉店するので鍵が受け取れなくなってしまいます」
  「**夜の到着OK** 20時30分までであればチェックイン可能です」

**「夜の到着OK」という見出しだけを読んで `ok` にしたとみられる。見出しは条件を落とす。**

マージ後は施設数286の維持、波括弧の対応、タグと `sauna_exists` の整合
（yes/room ならタグあり、shared/no/未調査ならタグなし）、総フィールド数の増減も見る。
**減ったときは件数ではなく消えた項目そのものを出す。** 訂正で消したのか
マージの取りこぼしかを見分けるため。訂正で消したときは `--allow-remove` を付ける。

### 値の書き方の規約

- **公式が範囲で書いている数値項目は上限を採る。**
  例: TORAMII「80〜90度まで自動上昇する電気式」→ `sauna_temp: 90`
- **選択肢のどの区分にも割り当てられない値は入れない。** 未調査に戻す。
  例: Oyado S の水風呂は掛け流しで通年変動し「10〜18」。
  `water_temp` は「夏場の水温」の区分なので割り当てられない
- **数値の区間は「下限を含み上限を含まない」。** 15℃ちょうどは `t1518`、
  18℃ちょうどは `t1822`、60分ちょうどは `m60`。
  2026-08 まで `wtemp` を「10〜15℃」「15〜18℃」、`heattime` を「30〜60分」
  「60〜90分」と書いており境界が重複していた。**実際に同じ15℃を
  HARUKA KANATA は `t1518`、Earthboat 宝台樹は `t1015` と記録しており
  不整合が生じていた。** 選択肢のラベル自体を以上／未満に書き換えて解消済み
- **`coldbath` の `tub` と `bath` は「名詞が何か」で決める。**
  括弧内の用途ではなく、設備そのものが浴槽なのか水風呂なのかを見る。

  | 記述 | 値 |
  |---|---|
  | 「**アウトドアバス**（水風呂・お湯張り可能)」（id=236） | `tub` |
  | 「浴室に2つ**浴槽**があり…もう1つは水風呂、もしくはお湯を入れることができます」（id=144） | `tub` |
  | 「**水風呂**（温水利用可）」（id=271 / id=122） | `bath` |
  | 「浴槽は二つあり、温泉と**水風呂**を行き来する」（id=212） | `bath` |
  | 「サウナを楽しんだら、**ジャグジー**でクールダウン」（id=193。公式全ページに「水風呂」の語が無い） | `tub` |

  浴槽が水風呂を兼ねるなら `tub`、水風呂に温水も入れられるだけなら `bath`。
- **複数棟の `capacity` を合算するかは、同じ運営の先例に揃える。**
  `global-stays.jp` は id=90 totonoco が「最大収容人数：18名（プライベート
  ヴィラ3名×6室）」に対し per-unit の 3 を採っているので、同じ書式の
  id=89 景雅（「8名（スイート5名×1室／スタンダード3名×1室）」）も合算しない。
  合算するのは公式が「**2棟利用**で最大N名」のように**まとめて使う前提の
  数字**を出している場合だけ（id=45 HARUKA KANATA → 17、id=93 → 11）。
- **この「最大を採る」は数値項目にだけ使える。カテゴリ値には使えない。**
  `capacity` で棟別の最大を採るのは「施設全体で最大N名が泊まれる」という
  集約に意味があるからで、`coldbath` や `sauna_type` のような選択肢型には
  「最大」に当たるものが無い。**棟で違えば入れない**（id=113 の `coldbath`、
  id=38 と id=130 の `sauna_type` はいずれもこれで削除した）。

---

## サウナ有無の判定基準

`sauna_exists` は4値。**判定基準は「客室内にサウナがあるか」**。

| 値 | 条件 |
|---|---|
| `yes` | 客室・棟にサウナがある。1組貸切なら敷地内の別棟サウナも含む |
| `room` | 一部の客室・棟のみサウナ付き |
| `shared` | 共用施設のサウナのみ。日帰り客も使う場合を含む |
| `no` | サウナなし（提供終了を含む） |
| 未設定 | 未調査。あり／なしを断定できない場合はこれに戻す |

### 判定のコツ

**サウナがどのページにあるかを最初に見る。** 客室ページにあれば客室設備、
施設紹介ページで「フロント／トイレ／レストラン」等と並んでいれば共用設備。
これで `shared` を10件以上見つけている。

**別棟＝共用ではない。** 1日1組貸切なら敷地内の別棟サウナも占有。
逆に「同一住所・同一運営」でも営業時間や客層が別なら共用（成田さくら邸）。

**予約制・時間制・別料金は共用のサイン**だが決定的ではない。
宿泊者が滞在中いつでも使えるなら `yes`（HARUKA KANATA）。

---

## 繰り返し確認された注意点

### 居室の暖房ストーブをサウナの熱源と取り違える（通算15件）

**`stove` でいちばん多い事故。** 一棟貸しの施設はリビングに薪ストーブや
暖炉を置いていることが多く、設備一覧では両者が並んで出てくる。

| 施設 | 実態 |
|---|---|
| id=167 箱根リゾートyamaki | 「Wood Stove／薪ストーブ」がバレルサウナとは**別の見出し**。説明は「薪が燃える炎の灯りは1/fゆらぎによる癒し効果」 |
| id=123 RIVER VIEW HOUSE | 「土間リビングに一歩入ると、薪ストーブの炎が迎える」。サウナ本体はサウナイキタイに「電気」と明記 |
| id=96 yl&Co.Hotel | 一休「1階は屋内に薪ストーブもございます」＝焚火台とセットの居室設備 |
| id=264/265 北軽井沢 | 家電製品欄の「ストーブ」がTV・洗濯機・冷蔵庫と並んでいる |
| id=268 ポーラーハウス | 「薪ストーブ（使用期間 12月～3月）」が「床暖房（使用期間 12月～3月）」と並記 |
| id=210 熱海オーシャンハウス | ハウスルール「薪ストーブの設置がありますが、ご利用いただけません」 |
| id=180 GLAMDAY 山ノ麓 | 「憧れの薪ストーブ」の見出しで「リビングルームにしつらえた薪ストーブ…**お部屋全体を温めるストーブ**」 |
| id=0 るうふ 揺之家 | **ページに「薪ストーブ」の語が無い。**「空調設備：ペレットストーブ、エアコン」だけ |
| id=35 by the river Isumi | 「ソファーや『Morso』の薪ストーブ、プロジェクターが備わり」＝リビングの家具 |
| id=54 Zekkei stay ISUMI | 安全注意「手すりがないため…また薪ストーブもございますので火傷にはご注意ください。・川沿いの柵が低いので」 |
| id=157 TANZAWA seven lanes | リビングの「大きな薪ストーブ」は「**大空間を暖めます**」と明記。テントサウナ側の設備欄は「ストーブ／ロウリュセット／ベンチ」で燃料の記載が無い |
| id=191 軽井沢 HOUSE VILLA | 公式トップ「全棟に薪ストーブ」。**サウナが無い Villa A・足湯棟・檜風呂棟を含む6棟すべての設備一覧**に並ぶ（DBに入っていた値を削除） |
| id=213 熱海別邸 双梅庵 | ハウスルール「薪ストーブの使用について 不可」。ページ内にサウナの記載自体が無い |
| id=257 THE GLAMPING 箱根十国峠 | 「薪の追加無料」「薪2束付き」は、BBQグリル・焚火台とセットで**全客室タイプ共通**の焚火用 |

**逆に採用できる書き方はこれ。** 同じ「るうふ」ブランドの id=4 鴨川919 は

  「薪ストーブのサウナや水風呂を完備」「薪ストーブのサウナで汗をかき」

と**サウナ自体を修飾している**。id=0 と id=4 が同一ブランドで正反対になった。

**メーカー名だけでも決められない。** HARVIA・TYLO・HUUM は薪式と電気式の
両方を製造しており、「Harviaのサウナストーブ」では熱源が確定しない
（id=215 / id=225 / id=56 / id=283 で採用を見送った）。

**運用の記述からも決められる（2026-09 に追加）。** 薪なら「薪をくべる」「着火する」に
なるはずの操作が、スイッチ操作と書かれていれば電気式。

  FAQ「サウナの操作はどのようにしますか？」→「お客様による**スイッチの操作**が
  必要です。」（id=47 Hackberry Holiday Home）→ `electric`

同じページの設備紹介は「フィンランド製のサウナストーブを設置しています。」だけで
メーカー名すら無く、それ単独では確定できなかった。

**型番まで書かれていれば確定できる。ただし必ずメーカーの製品情報に当たること。**

  「HARVIAのCILINDROを採用しています」（id=111）→ Cilindro は電気専用
    （PC60E / PC132E など。薪式のラインナップが無い）→ `electric`

  「Harviaの『LEGEND15』を採用」（id=101）→ **Legend は薪式**
    （harvia.jp は LEGEND150 を型番 WK150LD で掲載。WK は薪式）

  **id=101 ではエージェントが代理店サイトの表記を「電気ストーブ 13.8kW」と
  読んで electric と判定したが、同じ代理店の実際の掲載は
  「Harvia LEGEND150｜16kW相当｜サウナストーブ｜薪ストーブ」だった。**
  なお id=101 は wood にも書き換えていない。公式の表記「LEGEND15」が
  型番「LEGEND150」と完全一致せず、公式ページに薪の扱い（薪割り・薪をくべる）
  への言及も一度も無いため、型番の読み替えだけで断定するのは危うい。

### `stove` の出典なし値はブランドで固まっていた（2026-09 に全件決着）

出典URLの無い `stove` 34件を並べると、施設ごとに調べた値ではないことが
一目で分かった。

| ブランド | 件数 | 当時の値 | 決着 |
|---|---|---|---|
| マイグレ | 13 | 全て electric | **正しかった**（ikitai で14/14が電気） |
| COCO VILLA | 5 | 全て electric | **正しかった**（公式の施設別ページで確認） |
| Earthboat | 5 | wood 4 / electric 1 | **一律適用ではなく実態だった** |

**マイグレの2施設を個別に確認したところ、公式は「Harviaのサウナストーブ」と
しか書いておらず熱源を確定できなかった。** 初期投入がブランド単位で
値を振った疑いが濃かった。

**サウナイキタイのサウナ室タグが熱源を持っていた。** 掲載のあった14施設すべてが
「**ドライサウナ　対流式（ストーン）　電気　TV無**」で、**ブランド内で1件も
割れていない。** 値は変えず出典だけを付けた。
**`stove` の出典なしは 34件 → 3件**（id=60 / id=74 / **id=228 マイグレパノラマ**。
パノラマだけブランド15施設で唯一サウナイキタイに掲載が無い）。

**Earthboat の「wood 4 / electric 1」は初期投入の取りこぼしに見えたが、実態だった。**
5件とも公式の拠点別ページ（`earthboat.jp/nasu` ほか）を出典に持ち、サウナイキタイの
タグも独立して裏付けた。**id=271 埼玉川島だけがタグ「電気」で、他4拠点は「薪」。**
id=195 黒姫は備考「**自らで薪をくべて火を入れて…**」まで一致している。

**「ブランドで固まっている」は出典なし値を見つける嗅覚としては当たったが、
値そのものの誤りを意味しなかった。** 3ブランド23件のうち訂正が必要だったものは
**0件**で、**全部が正しく、出典が無かっただけ**だった。これは
「出典なしは疑うべきだが9割は正しい」（後述）と同じ結論になる。

**ただし2施設は熱源が単一ではなかった。** id=221 YEBISU と id=222 海の声は
サウナ室1「ドライサウナ・対流式（ストーン）・**電気**」（100℃／110℃）と
サウナ室2「ミストサウナ・格納式（ボナサウナ、座面、壁）・**ガス**」（45℃／47℃）の
**2室構成**。`stove` も `sauna_type` も単一値ではドライ側しか表せない。

**サウナの説明のなかに熱源が書かれているかを確認する。** 設備一覧に
「薪ストーブ」があるだけでは根拠にならない。id=167 と id=123 は
既にDBに入っていた値で、2026-08 に削除・訂正した。

同様に**「薪の香り」「木の香り」は熱源の根拠にならない**。
id=192 は遠赤外線サウナ、id=179 は口コミの情景描写だった。

### ブランド単位の一律適用は必ず間違える

同一ブランドでも棟・客室ごとに仕様が違う。以下すべてで確認済み。

- SANU 2nd Home: 建築タイプ（BEE/MOSS/RAY/SKY/ARC）別に備品が異なる
- アウトドア貸切別荘北軽井沢（id=264 / 265）: **公式URLもページ構造も同じ**なのに、
  264 は「ガーデン設備／…**テントサウナ**／…」、265 は「…**野外サウナ**／…」。
  書式が完全に同じでも表現が違う
- 軽井沢365: **5棟**すべて別仕様（リバーサイドヴィラ／フォレストテラス／
  フォレストガーデン／プレイガーデン大日向／ツリーネスト。定員は 12/9/8/7/5 名）。
  `/stay/riversidevilla` を開くと先頭に別棟の諸元が出るため読み取り注意
- 赤城宿 清芳山荘: 本館フィンランド／奥庫・質庫テントと客室で形式が違う
- Asile＆OLILI: Asile=バレル、OLILI=室内
- GIFTHOUSE: 那古海岸=3口IH・室内サウナ、洲宮=ガス1口・屋外薪バレル
- トライハク: 3施設中サウナがあるのは「かぐら」のみ

**書式の共通性は読み取りを速くするが、値の共通性は保証しない。**

**ただしタイプが特定できれば型どおりに使える。** SANU はサウナ解説ページで
「サウナ付きの施設は…**建築タイプごとに異なる仕様のサウナが設置されていますので、
各建築タイプのページでご確認ください。**」と明記している。施設→建築タイプ→タイプの
仕様、と**各段に名指しの出典があれば**結んでよい（id=177 / 178 は
`facility/sanu-cabin-with-sauna` が「MOSS Medium: …蓼科1st 4室」と名指ししている）。
**逆に id=246 では使わなかった。** RAY Medium には「露天温泉＆サウナ／露天温泉／
インナーバス＆サウナ」の3バリエーションがあり、RAY のサウナ記事は提供エリアが
館山1stのみ。伊豆1stは別バリエーションなので当てはまらない。

**選択状態がURLに載らないページを出典にするときは、選択状態も書き添える。**
SANU の備品リスト `2ndhome.sa-nu.com/supplies_list_new/sanucabin` は
建築タイプを選ぶドロップダウンを持つが、**URL末尾の `sanucabin` はどのタイプ名とも
一致せず、無選択だと BEE が既定表示になる。** 12施設がこのURLを `kitchen_type` の
出典に持っていた（`tools/shared_src.py` が検出）。4タイプを実際に切り替えて比べた
ところ調理家電はいずれも「三口IHクッキングヒーター」で値は誤りではなかったが、
出典としてはタイプを特定できていなかった。

### 公式サイトを唯一の正としない

**公式に記載がなくてもサウナが実在する例が5件**（PRIVE、FARM SIDE、
Sea by TORAMII、なすっぽ、THE LOOKOUT KUSATSU）。理由はサイトリニューアルでの
欠落、更新漏れ、訴求軸の違いなど。OTA やサウナイキタイで裏を取ること。

**サイト内で数値が食い違う例が6件**（GIFTHOUSE那古海岸のチェックイン時刻、
On the wave のサウナ定員、&SUN Laie back のキッチン、DAIGO SAUNA の定員、
UMInoTERRACE の温度、OYADO IKOI の人数）。複数箇所を突き合わせること。

**3面あれば3通り出る。** id=197 トライハク神楽のドッグラン面積は、公式本文が
「400㎡」、同じページの諸元表が「250㎡」、予約エンジンが「350㎡」だった。
**「2つのうちどちらが正しいか」という問題設定自体が間違っていることがある。**

**数値以外でも起きる。id=99 ハンズアウトドアリゾートは現行ページが「バレルサウナ」、
FAQ が「テントサウナ」だった。** 「切り替えました」という告知は無かったが、
**日付のある材料の偏りで決着できた。**

  バレル側 … 2023-04-24 の開設告知、**2026-09-03 の値上げ告知**（現役稼働の証拠）、
    本日の Activities Tour ページに単独掲載
  テント側 … FAQ に更新日の表示なし。サウナイキタイの投稿2件も2023年で
    バレル開設と同時期

**告知が無いまま古い記述が残るほうが普通なので、日付のある材料を探す。**

### サウナイキタイは強力だが古くなる

温度・サウナ定員・水温・水深・休憩イスを構造化して持っており、
**公式がまず書かない項目がまとめて取れる**（1施設から8項目取れた例がある）。
`water_depth` `water_temp` `chiller` はこれで初めて埋まった。

ただし投稿ベースなので**改装に追随しない**。
Six on the Beach TORAMII は公式に

> 2024年7月からこれまでのモルジュテントサウナ＊薪ストーブから、
> オリジナル小屋＊電気ストーブにグレードアップいたしました

とあるのに、サウナイキタイは今も「テントサウナ／薪」のまま。
**公式と食い違ったら公式を採る。** サウナイキタイは公式が黙っている項目の
補完に使う。数値も改装前のものが残っている可能性があるので、
サウナ本体の仕様（形式・熱源・定員・温度）が公式と違うときは他の数値も疑うこと。

**WebFetch は 403/405 で拒否される。** 実機ブラウザなら読める。
WebSearch のスニペット経由だと**別施設の数値が混ざる事故が複数回起きた**ので、
独立した2回以上の検索で一致した値だけ採ること。

#### 「無し」（✕）は否定の根拠にならない。「有り」だけが根拠になる

**チェック欄の「無し」は未設定の既定表示であって、「無い」と記録された値ではない。**

証明は id=70 The Pacific Retreat TATEYAMA。**イキタイ0・サ活0 で誰も情報を
入れていないのに、ロウリュ／オートロウリュ／セルフロウリュ／外気浴／休憩スペース／
ウィスキング／イオンウォーターの7項目すべてが「無し」と表示される。**
`img` の `alt` は「有り」「無し」の2値しかなく、チェックボックスの既定がオフというだけ。

2026-09 にエージェントが id=60 を「**外気浴×の明確なマーク**」と報告し、
`outdoor_rest: no` / `rest_chair: none` を提案してきた。**本当なら
「なし」を一度も記録していない5項目で初めての否定値**になるので確かめて、上の反証が出た。
**この発見で4つの判断を取り下げた**（id=60 の2件は記録前に却下、**id=13 の
`loyly: no` は記録済みだったので削除**、id=16 と id=31 の「食い違い」は未入力だった）。

なお `設備・ルール` 欄（Wi-Fi・駐車場・タトゥーなど）は `○` と `-` の表示で、
これも `-` は不記載として扱う。

**ただし備考の自由記述にある否定文は別物で、根拠になる。** チェック欄が
「未設定の既定値」なのに対し、こちらは投稿者が書いた文である。

  「**チラーなどはないので**季節に依って水温は変動します」（id=48）→ `chiller: no`

**サウナイキタイから取れた初めての否定値で、`chiller` の否定値4件のうち1件。**
同じ走査で肯定側も取れている（id=58「常設してある**冷却装置（チラー）**をお使いください」）。

#### 情報源を1つに固定して多数の施設に当てる「一括の波」が効く

2026-09 に波の形を変えた。公式サイトを1施設ずつ深く読む従来型は、V14-14 で
**10施設48分・308ツール呼び出し**だった。サウナイキタイ一括型は
**1施設あたり検索1回、ヒットしたページだけ精読**なので桁違いに速い。
**掲載が無いことを確かめること自体が成果**なので、外れたら即座に次へ進ませる。

**2026-09 に全国255施設を走査し終えた（未走査0件）。** 掲載率は千葉59% / 山梨59% /
静岡62% と地域を変えても動かなかった。着手前の見積もりは20%で、**3倍外していた。**
**オーナー調査待ちだと思っていた項目が机上で埋まる。**

| 項目 | 走査前 | 現在 | | 項目 | 走査前 | 現在 |
|---|---|---|---|---|---|---|
| `water_depth` | 1 | **72** | | `rest_chair` | 37 | **94** |
| `water_temp` | 11 | **85** | | `stove` | 109 | **157** |
| `sauna_temp` | 26 | **118** | | `loyly` | 145 | **165** |
| `sauna_cap` | 46 | **128** | | `outdoor_rest` | 176 | **198** |

**上位5項目はすべて設計書がチャネルC（オーナー調査）に置いていたもの。**
この走査だけでチャネルCの充足が 7.9% → 11.8% に動いた。

対象は `python3 tools/blanks.py --include-done --yield --ikitai` で出す。
**走査済みの判定は `data/ikitai-checked.json` だけで行う。** 掲載が無かった施設には
出典URLが残らないので、spec-data.js の出典で代用すると「調べたが載っていなかった」と
「まだ調べていない」を区別できず、同じ施設を何度も調べさせる（実際に千葉36件・
山梨23件が再掲された）。

**収量は波が進むほど上がった。** 千葉1.7項目/施設 → 静岡2.3 → 栃木・群馬2.4。
理由は市町村一覧の使い方が定着したため。**キーワード検索は施設名か市町村名にしか
当たらず、住所を番地まで入れると0件になる。** 1つの市町村の一覧を開けば
その地域の施設リンクがまとめて取れるので、**最後の波は26施設を34ツール呼び出しで
終えた**（1施設あたり1.3回）。

#### まず施設を同定する。番地の一致だけを条件にすると取り逃す

**同定の条件は「番地の一致」だけではない。固有のURL・号数・棟名でも同定できる。**
番地の表記ゆれは珍しくなく、番地だけを条件にすると**掲載されている施設を
「別施設」として捨ててしまう。** 全国走査で4件がこれに当たった。

| 施設 | DB の番地 | ikitai の番地 | 同定の根拠 |
|---|---|---|---|
| id=114 エンゼルフォレスト那須 | 高久丙3243-**474** | 高久3242-**153** | HPリンクが公式 `ang-ns.com` と完全一致 |
| id=268 ポーラーハウス南軽井沢1 | 西野牧**12514-9** | 西野牧**4-12514-1** | 固有の号数「**和美別荘4-14**」が完全一致 |
| id=279 DAIGO SAUNA | 大子354-**11** | 大子354-**14** | HPが施設固有ドメイン `daigo-sauna.jp` |
| id=277 | 宮中**4813-1** | 宮中**4812 他** | 「他」は複数区画を示す表記 |

**逆に、ブランド共通のドメインは識別子にならない。** id=275 は HPリンクが
`m-garden.jp` のブランドルートで、同ブランドの隣接する別邸と区別できなかった。

#### 見送る型

- **男・女・男女共用のタブが分かれている施設**は共用の大浴場の疑い。id=28 は
  営業時間が「（**大浴場**）月〜日 10:00〜17:00」で男110℃／女85℃と数値も違った。
  **全項目を見送り、`sauna_exists` の再確認対象にする**
- **A棟B棟が1ページに統合されている施設**（id=57 Refwind）。温度90℃・定員2人が
  どちらの棟か分からない。**数値項目でも帰属不明なら入れない**
- **同じ欄の中で構造化データと自由記述が別のことを言う。** id=143 は構造化が
  「水深80~110cm」なのに備考が「肩まで浸かれるプール」。**構造化された値を優先する**
- **サ活（利用者の投稿）は出典に使えない。** 「チラーはないので…」「保冷剤が20個」
  といった記述が3件出たがすべて見送った
- **棟・部屋を限定して書かれたページ**は全項目を見送る。備考に出るとは限らず、
  **住所欄そのものに書かれていることがある**（「MONS GORA SOLIS棟限定」）。
  備考型は「シロスイートに設置」など
- **サウナ室が3つ以上ある施設**は単一値で表せない。id=277 は4室
  （共用2＋**宿泊者専用2が70℃と90℃に割れる**）、id=281 は男女各4の計8室。
  2室でも仕様が違えば同じ（id=221 / id=222 のドライ電気＋ミストガス）
- **同一施設に登録が2件ある**ことがある。id=122 Earthboat Nasu は19棟の
  トレーラーハウス施設で、`/saunas/104704`（100℃・17℃・水深80~110cm）と
  `/saunas/119620`（**90℃・18℃・水深40~60cm**）が別々に登録されていた。
  **別の棟を別の投稿者が登録したとみられる**

#### 食い違いの扱いは項目で分ける

**温度・熱源・形式が公式と食い違ったら、その施設の値は全部見送る。**
設備そのものが別物（改装前・別棟）である可能性が高く、他の数値も信用できない。
id=170 がこれで全項目見送りになった。

**定員だけが食い違う場合は、他の値を採ってフラグを立てる。** 定員の差は
**数え方の違い**（快適に座れる人数か、詰めて入れる最大か）であることが多く、
設備が別物であることを意味しない。id=276 は定員以外の4項目がすべて一致していた
（他に id=193 / id=270）。

**タブが複数ある施設は、全タブで一致する項目だけ採る。** 全部一致するなら
それは設備の違いではなく表示上の分割（id=92 / id=106 / id=152 / id=267）。

#### `sauna_type` はサウナイキタイから採らない（両方向で誤りが確認された）

**「アウトドアサウナ」欄にバレルの選択肢が無く、屋外の小型サウナ建屋はすべて
「サウナ小屋（屋外・水着着用）」に丸められる。** 選択肢は テントサウナ3種／サウナ小屋／
サウナカー／川・滝・湖・海が水風呂 の10個だけ。id=93 はタグが「サウナ小屋」なのに
同じページの料金表が「**バレルサウナ**＋水風呂 貸切」だった。

**テントのタグ自体が誤っていることもある。** id=217 マイグレフラットは ikitai が
「テントサウナ（常設・定期設置）○」で備考も「テントサウナ横に…」だったが、
公式 maigre.jp/flat は

  「縁側直結の広いウッドデッキはオーナーのこだわりが詰まった**小屋サウナ**」
  「**サウナ小屋**横には漆黒のオーバーヘッドシャワーにスタイリッシュなバスタブ」

と明記していた。**`hut` が正しい。** エージェントは `tent` への訂正を提案してきた。

**したがってタグは値の根拠に使わない。** 備考の本文が「屋内サウナ」「サウナ小屋」等と
**明言している場合だけ**使える（id=9 sendouQ「2020年に追加されたサウナ小屋」、
id=219「本格的なサウナ小屋と、足を伸ばせるバスタブ２つを併設」）。
`indoor` はこの欄が屋外設備の一覧なので**そもそも導けない**。

#### 備考は公式と同一とは限らない。投稿者が書き足している

2026-09 に `coldbath` を7施設 `bath` → `tub` に書き換えかけて止めた。
ikitai の水風呂備考がマイグレ系で繰り返しこう書いている。

  「スタイリッシュな**バスタブ**２槽を設置…水風呂の他、蛇口を変えれば
    **天然温泉の露天風呂としても**」

規約「浴槽が水風呂を兼ねるなら `tub`」に当てはまるように見える。**公式を4件開いた。**

| 施設 | 公式の記述 |
|---|---|
| id=217 flat | 「スタイリッシュな**バスタブ**」「冷水浴を。**天然温泉の露天風呂としても**」 |
| id=223 kennys | 「大型の円形**バス**と２基の**浴槽**を備え、露天風呂もお楽しみいただけます」 |
| id=215 terrace | **「水風呂」と「天然温泉の露天風呂」を別設備として列挙** |
| id=219 ikki | 設備一覧「露天風呂(伊東天然温泉)・**水風呂**・オーバーヘッドシャワー・内風呂」＋本文「〜としても」 |

**設備一覧は水風呂を独立項目として数え、本文は同一設備の使い分けとも読める。**
ブランド共通の定型文でどちらとも取れるため、**公式を出典に持つ既存値は覆さなかった。**

**id=215 では ikitai の本文が公式と食い違っていた。** ikitai は「バスタブ…お湯も出るので
温度調節可能。露天風呂としても楽しめ」と書くが、公式は両者を別設備として列挙している。
**ikitai の備考を公式の引用として扱わないこと。**

#### `water_depth` は cm 帯。読み替え表はサウナイキタイ自身が持っている

2026-09 に選択肢を体感区分（膝/腰/胸/肩）から**サウナイキタイの cm 帯**に置き換えた。
**この項目を埋められる情報源がサウナイキタイだけ**で、そこが cm で持っているため。

検索の絞り込みUIが cm と体感を併記している。**推測で読み替えると外れる。**

| 帯 | 体感 | | 帯 | 体感 |
|---|---|---|---|---|
| `d2040` 20〜40cm | 脛 | | `d80110` 80〜110cm | **腰** |
| `d4060` 40〜60cm | 膝 | | `d110140` 110〜140cm | **胸** |
| `d6080` 60〜80cm | 股下 | | `d140` 140cm〜 | **肩** |

**既存の唯一の値が誤りだった。** id=143 の `shoulder` は備考「肩まで浸かれる
プール」から採ったものだが、構造化データは「水深80~110cm」＝**腰**。
施設の「肩まで浸かれる」は座って浸かる体感の表現で、水深の区分とは一致しない。

### 楽天トラベルの設備ページは検索が要らない（2026-09 に全件走査）

サウナイキタイを使い切ったあとの情報源。**「掲載の有無を調べる」工程がそもそも無い。**
DB の `ota.rakuten` が施設番号を持っているので、次のURLが機械的に決まる。

```
https://travel.rakuten.co.jp/HOTEL/{no}/{no}_std.html   ← 設備・アメニティ・基本情報
```

`ota.rakuten` には3つの書式があるので、どれからでも番号を取る。

```
travel.rakuten.co.jp/HOTEL/{no}/{no}.html
hotel.travel.rakuten.co.jp/hinfo/{no}/
hotel.travel.rakuten.co.jp/hotelinfo/plan/{no}
```

**curl で読める。** サウナイキタイと違って実機ブラウザが要らないので、192施設を
1施設1リクエストで取り切れる。**エージェントの判断を1件も挟まずに済む。**

#### マークアップが「楽天のマスタ」と「宿の自由文」を分けている

```html
<li data-locate="hotel-facility">     館内設備
  <ul class="eqHght"> … 楽天の項目マスタ（チェックボックス）
  <ul class="lnBrk">  … 宿が入力した自由文
<li data-locate="hotel-amenity">      部屋設備・備品
  <ul class="eqHght"> … 同じくマスタ
```

**この区別がそのまま信頼度の差になる。**

| | 既存値との照合 | 使い方 |
|---|---|---|
| `eqHght`（マスタ） | **249セルで食い違い0件** | そのまま採ってよい |
| `lnBrk`（自由文） | **別施設の内容が混ざる** | 公式で裏を取るまで採らない |

**切り出しに `</li>` を終端として使ってはいけない。** 中の `<ul><li>` 項目が先に
閉じるので、非貪欲マッチだと1項目目で切れる。実際それで189件すべてが
「設備リストが空」になった（エラーは出ず、件数を見て気づいた）。
**次の `data-locate` までを切り出す。**

#### `eqHght` から採れるもの

| 項目 | 根拠 | 入れられる値 |
|---|---|---|
| `bring_towel` | 部屋設備に「タオル」「バスタオル」 | `ready` のみ |
| `bring_amenity` | 「シャンプー」「ボディーソープ」「ハミガキセット」等 | `ready` のみ |
| `wifi` | 「インターネット接続(無線LAN形式)」 | `yes` のみ |
| `pet_ok` | 館内設備「ペット同宿可」 | `yes` のみ |

**一覧に無いことは「なし」ではない。** 宿がチェックを入れなかっただけで、
サウナイキタイの「無し」と同じ未設定の既定表示。**肯定側しか作らない。**

2026-09 の走査で `bring_towel` +95 / `bring_amenity` +121 / `wifi` +17 の
**233セルを130施設に記録し、既存値との食い違いは0件**だった。`pet_ok` は新規0だが
**40件の既存値をそのまま裏付けた。** 投稿ベースのサウナイキタイ（温度・定員で
何度も食い違った）と違い、**運営自身が登録した項目マスタ**だからとみられる。

#### `lnBrk`（自由文）は別施設の内容が混ざる

id=236 Tiny Base The MOUNTAiN の自由文は、**同じ枠の2行で正誤が割れた。**

  「**IHコンロ**、食洗機、炊飯器、トースター」 … **誤り**
  「サウナはフィンランド式サウナストーブ（HARVIA社・**薪式**）」 … 正しい

公式 tinybase.co.jp/stay/ の「Information」は3タブに分かれている。

  **MOUNTAiN** タブ … 「フィンランド式サウナ(**薪**)」「冷凍冷蔵庫 / **ガスコンロ** / 浄水器」
  **Reception / City Area** タブ（SEA/ROCK/TRee/HOUSE/TRAILER/Valley）
    … 「**IHコンロ**…◇**Valleyのみ**： ガスコンロ/ **食洗機**」

楽天の「IHコンロ、食洗機」は City Area 側の内容である。**既存値 `gas` を書き換えなかった。**
サウナの行は公式と一致しており、**CLAUDE.md が「メーカー名だけでは熱源が確定しない」と
してきた HARVIA の例が id=236（薪）／id=237（電気）の2件とも裏付けられた。**

**楽天が用意した定型文も混ざる。`kitchen_type` の根拠にしてはいけない。**

  「キッチン/調理器具/食器/洗濯機/乾燥機/電子レンジ/エアコン」… 19施設に同一
  「調理道具/食器/洗濯機/乾燥機/電子レンジ/エアコン/キッチン」… 8施設に同一
  「※部屋によって設備が変わる場合がございます。」… 36施設に同一

**2施設以上に同一の行が出たら定型文として弾く。**

#### `late_arrival` は取れるが入れない

「最終チェックイン：HH:MM」が182施設にあり、22時以降の74施設を `late_arrival: ok` に
できる。**入れていない。楽天の「最終チェックイン」は予約の受付上限であって
施設の方針ではない**（OTA の「定員 1名〜9名」と同じ型）。

#### 住所が同じページに載っているので照合が無料で付いてくる

192施設ぶんの独立した住所が取れる。**ota リンクの誤りが2件、住所の誤りが2件出た。**
照合は「番地の一致」または「楽天の住所がDBの住所で始まること」。
大字・字・郡の有無や施設名の付加は表記ゆれとして吸収する
（これを入れる前は33件が不一致に見えた。実際は9件）。

#### 楽天は「掲載終了」と「404」を出し分ける

**掲載終了は HTTP 200 で案内ページを返す。** ステータスだけでは死活を判定できない。
本文の「指定されたページは掲載終了しました」を見ること。
**検索索引には死んだURLがクチコミ・写真・地図のサブページまで残る。**

2026-09 に3件見つかり（id=83 掲載終了 / id=278 掲載終了 / id=285 404）、
**id=278 は楽天トラベルキャンプ `camp.travel.rakuten.co.jp/properties/1024` に
移っていた**（住所が一致。ドメインが `travel.rakuten.co.jp` でなくても楽天の施設ページ。
id=26 の `vacation-stay.jp` と同じ扱い）。残る2件は削除した。

### じゃらん・なっぷは設備の仕様を持たない（2026-09 に判定して却下）

**波にする前に1施設で確かめる。効かないと分かった時点で止めるほうが安い。**

#### じゃらん `https://www.jalan.net/yad{no}/`

**文字コードが Shift_JIS。UTF-8 で読むと全部文字化けする**（curl は通る）。

| 欄 | 内容 | 使えるか |
|---|---|---|
| アメニティ・施設・サービス | **○/× を明示**（タオル／バスタオル／シャンプー／ボディソープ／歯ブラシ／石けん／くし・ブラシ／髭剃り／浴衣／パジャマ／リンス／綿棒 ほか） | **`bring_towel` `bring_amenity` の否定値が作れる** |
| 温泉・風呂 | 「温泉 無し」「**サウナ 男/女/混浴 の室数**」 | 大浴場型の判別に使える |
| インターネット関連 | 「[接続方法] 無線LAN [インターネット接続] 無料」 | `wifi` |
| 標準的なチェックイン/アウト時間 | 「16:00〜」「〜10:00」 | **開始のみ** |
| **コンロ・キッチン** | **欄が無い** | ✕ |
| **サウナの形式・熱源・ロウリュ** | **欄が無い** | ✕ |

**id=281 はコンドミニアム（キッチン付きが前提）なのに火口の記載が無い。**
一方 「サウナ 男4・女4」はサウナイキタイの走査結果（男女各4の計8室）と一致した。

**否定値の収量は小さいと見込まれる。** 2施設ともタオル・シャンプーは○で、
×が付くのは石けん・浴衣・髭剃りだった。現状 `bring_towel` は171:1、
`bring_amenity` は201:0 で `ready` に偏っている。
**DB が持つ jalan リンクは2件だけ**なので、使うなら1施設1検索で yad 番号を引く。

#### なっぷ `https://www.nap-camp.com/{県}/{no}`

**キャンプ場向けの語彙しか持たない。** 場内設備は AC電源 / シャワー /
ウォッシュレット式トイレ / 炊事棟 / ゴミ捨て場。コンロの語は**レンタル料金表の
自由文**にしか出ない（「キッチンセット：6,600円 {…**ガスコンロ**}」＝貸出機材で
備え付けではない）。**そもそもキャンプ場しか載っていない。**

### 出典URLの無い値は初期一括投入のコホートで、誤りが集中している

2026-08 に全走査したところ、**desk 値1618件のうち出典URLが無い566件は
すべて `at: '2026-07'`** だった。逆に 2026-08 の値1052件は全件に出典がある。
つまり「url が無い」だけで初期投入分を正確に切り出せる。

| 項目 | 出典なし | 項目 | 出典なし |
|---|---|---|---|
| capacity | 170/286 (59%) | sauna_type | 43/124 (35%) |
| pet_ok | 87/199 (44%) | coldbath | 45/151 (30%) |
| sauna_exists | 86/275 (31%) | loyly | 29/106 (27%) |
| stove | 44/93 (47%) | outdoor_rest | 24/115 (21%) |

**このコホートから実際に誤りが出ている。**

- `capacity=9` が64件（OTA の9名上限の機械的読み取り）
- id=229 WEAZER西伊豆の `sauna_exists=no`（実際は room）
- id=48 庄屋の里 古民家たなかの `pet_ok=yes`（一休に「× ペット可」）

調査で施設を開いたら、**空欄でない項目でも出典が無ければ疑う。**
`tools/blanks.py --prompt` が「出典なし（ついでに検証）」として
調査指示に併記するので、同じページを開くついでに検証させる。

**2026-08 に既存値40件を検証した結果、誤りは3件**（`stove` 2件が居室の
暖房ストーブとの混同、`capacity` 1件）。**残り37件は正しく、出典を
付けられた。疑うべきだが9割は正しい。**

**むしろ「出典があるのに中身が無い」ほうが危険。** id=252 伊豆高原
テントリゾートは `sauna_type: tent` / `stove: wood` に出典URLを持って
いたが、そのページに「サウナ」の語が一度も無かった。施設名の「テント」
（＝テント泊の宿泊タイプ）と紹介文の「BBQや焚き火」からの読み違いで、
validate.py が警告し続けていた不整合の正体だった。

**2例目が出た（id=204 オーシャンビュー南熱海）。** `capacity: 12` が公式ページを
出典に持っていたが、そのページの表示は「定員 8名」。DOM を直接見ても
`<p class="value">8名</p>` が1組あるだけで、全文に「12名」は無い。
**出典URLの存在は中身を保証しない。**

なお同じ resolstay.jp でも id=205 は「定員 10名（推奨8名）」という書式で、
**括弧付き併記を使うページと使わないページが混在している。** 併記を見落として
推奨人数を定員として読む事故が起きうるので、この2つの書式の違いに注意する。

**3例目（id=131 LEVATA）。** `kids_free: 6` が公式 levata.jp を出典に持っていたが、
**そのサイトの本文に「歳」「無料」「円」の3語が一度も出現しない。** 料金・年齢の記載が
どこにも無かった。単位は「歳まで」で、値6が定員6と一致していたのは単位取り違えの兆候。
削除して未調査に戻した。

**4例目は別の型だった（id=280 藤右衛門）。投入時は正しく、あとからページが
書き換わった。** 2026-08 は公式FAQ の「**チェックインは、15:00〜18:00です**」を
根拠に `late_arrival: no` としたが、**現在その FAQ は存在しない。**
「よくある質問」のリンクは `#faq` を指すが、そのアンカーに中身が無い。
現行の「ご利用案内」はこうなっている。

  「check チェックイン・アウト（**セルフ**） • **チェックイン：15:00〜**」
  「※**スマートキーのコード**は宿泊日当日にメールでお知らせします」

**終了時刻が書かれていない。** 無人チェックインに変わったとみられるが、
「21時以降に着いてよい」と書いた文も無い。不記載は肯定の根拠にも否定の根拠にも
ならないので**削除して未調査に戻した。**

**出典URLが生きていても、そのページの中身は入れ替わる。** 1〜3例目は
「投入時から誤読」だったが、これは**当時は正しかった値が、根拠の消滅で
支えを失った**型である。古い `at:` を持つ値を再確認するときは、
**同じURLを開いて同じ文が今もあるかを見ること。**

### DB の feature / desc は独立した裏付けなしに信頼しない

- primera villa: `feature` に「サウナ」とあるが公式サイトに記載がない。
  **ただし「実在しない」とは確定していない**（後述）
- VillaEL5: `feature`「6LDK 185㎡」と `desc`「7LDK 200㎡」が食い違う
- Asile＆OLILI: `capacity=9` の根拠が不明（Asile 8名／OLILI 14名）

### OTA の「定員」欄は実定員ではない（9 だけの問題ではない）

**`capacity=9` が64件あった原因はこれ。** 一休.com と Yahoo!トラベルの
「定員」欄は **9名が仕様上の上限**で、それ以上の施設でも「定員 1名～9名」と
しか表示されない。Noёl HAKONE GENSEN のページに明記がある。

> 本施設は最大20名様まで宿泊可能ですが、一休のサイト仕様上9名様しか
> 予約ができません。10名以上の場合は別途ご連絡下さい。

同ページのタイトルは「最大20名様、1棟貸しプラン」。初期の一括投入がこの欄を
機械的に読み取ったとみられる。2026-08 に45件を訂正して 64件 -> 21件になり、
9の突出は消えた（分布は 8名53 / 6名49 / 4名42 / 10名29 / 12名22 / 9名21）。

- **OTA の「定員 1名～9名」は根拠にならない。** 施設概要文やプラン名の
  「最大N名」、または施設自身の予約サイト（chillnn 等）を見ること
- **9 が正しい施設も実在する**（マイグレYEBISU / HOODSTAR、AMAO VILLA 富戸など。
  いずれも公式サイトに9と明記）。一律に誤りとしないこと
- **OTA の定員欄が「第3の数字」を出すこともある。** id=96 yl&Co.Hotel は公式が
  「Number of guests : 1 ~ 10 persons」、一休の部屋名が「一棟貸切最大10名様」なのに、
  **同じ一休の「定員」欄は「2名～7名」**だった。既存値は 5 で、3つとも違う。
  施設が書いた文（公式の諸元と部屋名）が一致する 10 を採った
- **料金が変わる人数を定員として読む誤りもある（新類型）。** id=196 Karuizawa
  Luxe Villa の `capacity=4` の出所は次の記述とみられる。

  > 「・4名様まで同一料金でご利用いただけます。
  >   ・5名様以上は、1名様につき7,700円（税込）の追加料金を頂戴いたします。」

  **5名以上も受け入れているので 4 は上限ではない。** ただし明確な上限記載が
  見当たらず正しい値が決まらない。`capacity` は index.html にも出て空欄に
  できないため 4 のまま残している。

  **2件目が出た（id=280 藤右衛門）。** 紹介文自身が「**4名様まで同料金。最大20名
  宿泊可能**な一棟貸し宿。」と書いており、既存値は 5 だった。公式に「最大定員：20名」の
  直接記載があり、母屋14名（ダブル2台＋シングル2台＋布団8組）＋蔵6名の内訳とも
  完全に合う。**20 に訂正した。**
- **9 以外の数字でも同じことが起きる。** id=6 CAP MARTIN Funny house は
  一休の部屋種別欄が「定員 1名〜5名」で、これがそのまま capacity=5 として
  投入されていた。公式FAQは「大人最大4名様」で、一休の施設からのお知らせと
  プラン名も「最大4名様」、ベッド構成（シングル×2＋キング×1）とも整合する。
  **「OTAの定員欄」全般を疑い、施設固有の「最大N名」を探すこと**

### 棟ごとに定員が違う施設の扱い

**公式が「全棟利用時は最大N名」という施設全体の数字を出していれば、
複数棟でもそれを採用する。** 単棟の数字しか無い場合だけ代表値を置かない。

  「2棟利用 6名〜14名用（※最大17名）」（id=45 HARUKA KANATA → 17）
  「2棟で大人11名まで宿泊可能」（id=93 ヴィラグリファーム七里岩 → 11）

どちらも既存値は単棟の数字を読んだもの（6 と 5）だった。
CLAUDE.md は以前 id=93 を「代表値を決められない」に分類していたが、
この基準で解ける。

### DB の紹介文（desc / feature）の人数は当てにならない

286施設の desc / feature から「最大N名」「定員N名」を抽出して capacity と
突き合わせると **24件が食い違う**。うち11件は出典URLの無い 2026-07 値。

**3件を一次情報で確認したところ、3件とも紹介文のほうが誤りだった。**

| 施設 | capacity | 紹介文 | 正 |
|---|---|---|---|
| id=215 マイグレテラス | 8 | 10 | **8**（公式「定員 8名」） |
| id=151 琥珀-AMBER- | 5 | 10 | **5**（公式「最大5人まで」） |
| id=45 HARUKA KANATA | 6 | 20 | **17**（どちらも誤り） |

過去に id=83 THE TIME FUJI と id=113 ASH Villa では紹介文のほうが正しかったが、
**それは偶然。この走査は「どちらかが誤っている施設」を絞り込むフラグとして
使い、値は必ず一次情報で決める。**

**同じページに載っている別施設の宣伝バナーから値を拾った例もある（id=215
マイグレテラス）。** `sauna_type: tent` の根拠になりうる「テント」の語は公式ページに
1箇所しかなく、それは姉妹施設の広告枠だった。

> 「『マイグレ天』は、本格的なフィンランド式テントサウナでロウリュを楽しめます」

テラス自身の記述は「緑に囲まれた庭のデッキにはオーナーのこだわりが詰まった
サウナと露天風呂」で構造の明記が無い。削除して未調査に戻した。
**引用を採るときは、その文が対象施設について書かれているかを確かめる。**

**紹介文が別施設のものだった例が出た（id=160 雅・仙石原）。** capacity=14 と
紹介文「定員8名」の食い違いは、紹介文が姉妹施設「雅・芦ノ湖 貸別荘」（同運営・
定員8名）の文章と一言一句一致していたため。`capacity=14` が正しい
（TRIPTO の見出し・本文・予約枠上限すべて14）。**紹介文に残っていた
「別荘地から芦ノ湖畔（海賊船乗り場）まで徒歩約2分」という芦ノ湖の説明は
差し替え済み**（2026-09 に確認。「海賊船」の語は全ページで0件）。

この見落としで一度誤った判断をしている。THE TIME FUJI は一休の「定員 1名～9名」
を根拠に9と確定してコミットしたが、施設自身の予約サイトに
「宿泊人数は7名様までとなっております。」とあり **7 が正しかった**。
DB の紹介文「最大7名までの隠れ家」のほうが合っていた。

### 公式URLの品質問題（286件中12件・約4%）

パターンは5種類。

| 類型 | 例 |
|---|---|
| 別施設のURL | THE TIME FUJI（訂正済） |
| 広告トラッキングパラメータ付き | enico 2件（訂正済） |
| 失効・404 | VillaEL5（訂正済） |
| アフィリエイト集約サイト | 天神郷 昊（訂正済） |
| リンク集のみ・実体なし | Avalon Cove、Villa Torami、Jade Group 3件（未訂正） |
| **ブランドのドメインルートで施設に到達しない** | **28施設（2026-09 に15件を個別ページへ差し替え済み）** |

**6つ目の類型は「壊れていない」ので長く見逃していた。** ブランドルートは 200 を返すし
ブランドも正しいが、**利用者が「公式サイト」を押すとトップに着き、そこから施設を
探し直すことになる。** SANU は12施設が同じURLを指していた。

**差し替え先は新たに調べる必要が無かった。** `spec-data.js` の `url:` に**同じドメインの
施設個別ページを既に出典として持っている施設が15**あり、調査で開いていたのに
`official` に反映されていなかっただけ。

残る13施設は差し替えられない。Hiire 3件は出典が共通の一覧ページ、terracecollections
2件は3施設を1ページに束ねる作り、Jade Group 2件はサイトが未編集のプレースホルダー、
SANU 6件は出典がエリアページ止まり、id=10 は出典が2棟のうち片方しか指さない。

**判定前に必ず DB の `addr` を確認する。** ページタイトルに引きずられて
&SUN Laie back を誤判定しかけた（「| 葉山」だが実際は南房総）。

**同名の別施設が実在する。** 2026-09 にエージェントが「じゃらんの住所と一致」と
報告してきた施設を確認したところ、じゃらん yad347487 の「OYADO S」は
**神奈川県鎌倉市大町**の施設で、DBの id=172（**箱根町元箱根**、公式は
oyados-**ashinoko**）とは別物だった。危うく定員を 12 → 6 に書き換えるところ
だった。**エージェントの「住所は一致した」という報告を鵜呑みにしない。**

### 同じ問題が `ota` にもある（2026-08 に走査 → 2026-09 に修復済み）

`official` だけの問題ではなかった。286施設の `ota` を走査したところ同じ類型が出た。
**方針は「差し替えを試み、見つからなければ消す」。** 実機で開いて住所を照合し、
一致したものだけ差し替える。2026-09 に38エントリを処理して差し替え14件・削除10件
（残りは共有元が正しく変更不要）。

**「別施設と共有」型はすべて『片方が正しく、もう片方が誤っていた』。**
両方消すのではなく誤った側だけ直せばよい（id=77/78、86/88、201/203、204/205、
245/246）。

**Airbnb 5件は 2026-09 に削除した**（id=14/118/126/227/228）。差し替え候補は
見つかっているが、Airbnb が自動ブラウザを拒否し（どのURLでも検索トップにリダイレクト、
Wayback にもキャッシュ無し）住所の実機照合ができず、検索索引のタイトル一致だけでは
規約を満たさない。**現状はどれも壊れたリンクが利用者に見えたままだったので、
方針どおり削除した。候補URLは `data/desk-research.js` に残してある。**
同じ調査中に id=204 で「COCO VILLA MINAMIATAMI」という**地名だけ同じ別ブランド施設**が
Agoda 検索に紛れ込んでいる（住所は伊東市網代）。

**共有の検出は完全一致ではなく、クエリ文字列を除いて比べること。** id=87
hotel norm. ao の booking は id=88 と同じ `norm-fuji` スラッグを指していたが、
末尾に `?label=...` が付いていたため文字列比較をすり抜け、全件修復で見落とした。
完全一致だと重複0組、クエリを除くと1組。**`validate.py` に検査を入れた。**

**ドメイン名だけで判定すると誤る。** id=26 の vacation-stay.jp は「ドメイン不一致」と
分類していたが、楽天LIFULL STAY の自社ブランド（Vacation STAY）で内容も正しかった。

**「壊れていない」URLも見る必要があった（2026-09）。** 全走査したところ
**楽天の ota 36件が広告計測URL**（`HOTEL/NNNNN/?s_kwcid=paidsearch&…`）だった。
施設ページには到達するので今まで見逃していたが、**利用者のクリックが広告計測に流れる**
うえ、**クエリを含んだままだとURLの比較ができない**（id=87 の見落としの原因）。
正規形 `HOTEL/NNNNN/NNNNN.html` が HTTP 200 を返すことを確認して36件を直した。

同じ走査で id=93 の `official` 末尾の「√」と、id=96 の expedia が
`Hotel-Search?` の検索結果ページであることも見つかった（後者は削除）。
**`validate.py` に URL の健全性検査（空白・検索結果ページ・広告計測URL・不正文字）を
入れた。**

**その検査が `s_kwcid` しか見ておらず、356件を取りこぼしていた（2026-09 に訂正）。**
全1203URLを走査すると **359件（30%）にクエリが付いていた**（Booking 164 / Airbnb 89 /
Agoda 68 / 一休 16 / Expedia 8 / 楽天 3 / official 8）。実害は3つある。

1. **過去の日付が埋め込まれている（53件）。** `checkin=2026-07-17`、一休の
   `cid=20260615&cod=20260616`、楽天の `f_nen1=2026&f_tuki1=06&f_hi1=15`。
   利用者は**もう過ぎた日付で検索した結果**の画面に着く。id=180 の booking URL は
   末尾が **`#no_availability_msg`** で、「空室なし」の画面を貼ったことまで分かる
2. **他人の検索セッションIDが公開ページに載る。** Booking の
   `sid=d7c8368027251ff7686409cb8cf4d4d5` が **162件で同一値**。`srpvid` `srepoch`
   `hpos` `sr_order` `dest_id` も同じ断片で、**1回の検索結果一覧から162施設ぶんの
   リンクを収集した**ことを示している
3. **クエリを含んだままだとURLの比較ができない**（id=87 の見落としの原因）

**パスだけで施設に到達することを各ホストで確認してから外す。** booking 202 /
ikyu・airbnb・agoda・rakuten・公式8件すべて 200。**除去で新たに完全一致になる組は
0件**だった。**施設を指すパスは変えない。**

**クエリが施設の特定に必要なものは外さない。** id=9 `?facility=1st`、
id=142 `?propid=283750`、id=196 `?hotelCode=0000002923` の3件。

**サイト自身のアフィリエイトIDと取り違えないこと。** かつて index.html のOTA設定欄に
「いまは施設名でのキーワード検索リンクになっています。アフィリエイト収入を得る場合は、
各 url の中身をあなたのアフィリエイト用リンクに差し替えてください」とあった
（**2026-09 に死んだコードごと削除。差し替え先は `tools/fix_villa.py` の
`set_villa` / `remove_ota` に変わっている**）。
**現在のクエリはすべて広告の着地URLの残骸で収益に関係しない。** Booking の
`label=New_Japanese_JA_JP_...` は37件で同一値、`aid=356980` も Booking 自身の
集客チャネルのもの。将来アフィリエイトリンクを入れたら、この検査の除外が要る。

**同じ走査で OTA の言語ロケールの問題が出た。**

| OTA | 状況 |
|---|---|
| Booking | 193件すべて `.ja.html`。問題なし |
| Agoda | 154件中 **`ja-jp` は55件だけ**だった（en-in 16 / en-nz 15 / en-sg 12 / en-ie 8 / en-gb 4 / en-za 3 / en-au 3 / en-ca 2 / zh-tw 1 / zh-cn 1、ロケール無し34）。**2026-09 に65件を `ja-jp` に訂正済み**（各件で HTTP 200 を確認）。ロケール無しの34件は接続元の言語に従うので触っていない |
| Expedia | 59件中 **34件が日本語でない**。ドメインが日本以外17件（.com 7 / .co.uk 2 / .com.my 2 / .com.hk 2 / .com.ph 1 / .ca 1 / .com.sg 1 / .com.au 1）、`.co.jp` だが `/en/` 付き17件。**未訂正** |

**日本語サイトの利用者を英語・中国語のページに送っている。** Agoda はロケールが
通貨の既定も決めるため価格表示にも影響する。**Booking は193件すべて `.ja.html` で
問題が無かった**ので、収集元によって混ざったとみられる。

**Expedia は自動アクセスに「人間認証」を返して 429 で遮断する**ため、Agoda のような
全件確認ができない。ブラウザで id=4（`.com.ph` → `.co.jp`）を開くと「鴨川９１９」が
日本語で正しく表示され住所も一致した。

**機械的な変換ほど1件ずつ目視すること。** `/en/` を落とすのに `path[4:]` と書いて
先頭のスラッシュまで削り、id=119 と id=276 が `https://www.expedia.co.jpSANU-...`
という壊れたURLになりかけた（`path[3:]` が正しい）。

| 類型 | 件数 | 例 |
|---|---|---|
| 別施設を指す | 6組 | id=201 の一休が id=203 のページ（実物で住所確認） |
| 広告クリック計測の中継URL `google.com/aclk` | 14エントリ/12施設 | id=3, 5, 14, 19, 28, 41, 63, 227, 242, 243, 248 |
| 検索結果・カテゴリページ | 4施設 | id=60/228 が `airbnb.jp/s/homes` |
| キーとドメインの不一致 | 7件 | id=19 booking→instagram、id=113 agoda→公式 |

`official` が複数施設で同じなのは**ブランド共通URLなので誤りとは限らない**
（SANU 12件、軽井沢365 3件など）。ただし Jade Group 3件はステージング環境と
別物件に誤誘導する。

### 住所は座標より信頼できない（2026-09 に全件監査）

**`addr` が壊れていても座標は正しい。** 座標は Google Places の `searchText` で
**施設名＋住所**から引いているため、住所の誤りに引きずられにくい。
この性質を使って住所の誤りを機械検出できる。

```
DBの addr を国土地理院のジオコーディングにかけ、data/coordinates.json の
座標との距離を測る。大きく離れる施設は addr が壊れている疑い。
```

**ただし距離が大きいことは、それだけでは誤りを意味しない。** 3km以上ずれた
24件の大半は、ジオコーダが番地まで解決できず大字や市町村の重心を返しただけ
だった。広い大字では重心距離が数kmになる。**距離は候補を絞るフラグであって
判定ではない。** 実際に誤っていたのは構造的な異常を併せ持つものだけだった。

| 類型 | 件数 | 例 |
|---|---|---|
| 都道府県名の重複 | 2 | 「**長野県群馬県**吾妻郡長野原町北軽井沢」（正しくは群馬県） |
| 町村名の欠落 | 2 | 「栃木県那須郡**高久乙**」（那須町が抜けている） |
| 別の大字を書いている | 1 | id=115「那珂川町**矢又**1948」（公式は馬頭360） |
| 全角ハイフンの混入 | 4 | 「－」「‐」「−」 |

町村名の欠落2件は**286件中この2件だけジオコーディング自体が失敗する**ので
すぐ分かった。訂正後は id=125 が番地まで解決して0.49km、
id=189 が42.82km→0.74kmになった。

**チャネルAの導出値は住所の誤りの影響を受けていない。** 標高・スーパー・
コンビニ・IC・駅・温泉はすべて座標から出しており、座標は正しかった。

**「不一致」に見えて正しいこともある。** id=43 Montevan は公式に住所が
2つ併記されており、DBの「香191-3」がヴィラ本体、一休が載せている
「大賀170-2」はチェックイン用のサービスデスクだった。

### この監査で検出できない型がある（2026-09 に id=239 で判明）

「addr が壊れていても座標は正しい」は **addr が『壊れている』場合の話**だった。
id=239 AMAO VILLA の addr は壊れていない。**実在する、姉妹施設の正しい住所**
（AMAO VILLA 伊豆高原の住所）だった。Places の `searchText` は施設名＋住所で引くので、
この場合は素直に姉妹施設の座標を返す。

```
data/coordinates.json の id=239        (34.901694, 139.123776)
旧 addr「富戸1007-32」をGSIで引いた点  (34.901443, 139.122162) … 0.15km
正しい「富戸911-122」をGSIで引いた点   (34.887718, 139.121674) … 1.57km
```

**保存座標は旧 addr と 0.15km で一致してしまう。** つまり「addr をジオコーディング
して座標と比べる」手法では**この型は検出できない**（両方が同じ誤りを共有している）。
検出の糸口は OTA ページの住所との食い違いだった。**外部の独立した住所（OTA、
Google マップの事業者情報）と突き合わせる必要がある。**

**この場合はチャネルAの導出値も誤る。** id=239 の標高・スーパー・コンビニ・IC・駅・
温泉は AMAO VILLA 伊豆高原のものになっている。

### 同じ運営の姉妹施設どうしでフィールドが混線する

id=239 は addr / feature / desc の3つが、それぞれ**別の姉妹施設のもの**だった。

| フィールド | 実際の帰属先 |
|---|---|
| `addr` 富戸1007-32 | AMAO VILLA **伊豆高原** |
| `feature` 「2024年に開業したばかりの**AMAO VILLA 城ヶ崎**は…」 | AMAO VILLA **城ヶ崎** |
| `desc` 「**熱海**の雄大な景色を…**熱海海上花火**を一年中楽しめます」 | AMAO VILLA **熱海** |

`official` と `ota.ikyu` は正しく富戸を指しており、`capacity` / `sauna_cap` /
`sauna_temp` も富戸公式と一致していた。**リンクと数値は正しいのに、住所と文章だけが
他施設のもの**という混ざり方をする。id=160 雅・仙石原（紹介文が姉妹施設のもの）と
同じ型で、**複数施設を運営するブランドでは初期投入時の取り違えを疑う。**

**3件目が出た（id=132/133 GEOSPOT MOTOHAKONE）。A棟と B棟で `addr` が入れ替わっていた。**
一休とサウナイキタイの4ページが完全に一致して A=103-243 / B=103-128 を示し、DBは逆だった。
DBが参照している一休URL（id=132→00052338、id=133→00052339）とも対応しているので、
リンクの取り違えではなく addr だけの取り違え。

| 施設 | 混線したフィールド |
|---|---|
| id=160 雅・仙石原 | `desc`（姉妹施設「雅・芦ノ湖」のもの） |
| id=239 AMAO VILLA | `addr` / `feature` / `desc` / **`ota.rakuten`** がそれぞれ別の姉妹施設のもの |
| id=132/133 GEOSPOT | `addr` が A と B で入れ替わり |
| id=275 ときわ邸 M-GARDEN | `addr` が隣の別邸「清香庵M-GARDEN」のもの（1-2-43。正は1-2-45） |
| id=19 GIFTHOUSE 那古海岸 | **`checkin`** が姉妹施設 id=20 洲宮の「15:00〜24:00」（正は15:00〜18:00） |
| id=155 SAJIMA Funny house | **`ota.rakuten`** が同ブランドの「ZUSHI Funny house」 |

**id=239 は4つ目のフィールドが出た（2026-09）。** `addr` / `feature` / `desc` を
直したあとも `ota.rakuten` が「AMAO VILLA 熱海」を指したままだった。
**1つ直しても他が残っていると考える。**

**id=19 は自分の記録に書いてあったのに反映していなかった。** 2026-08 の一次記録に

  「那古海岸は『3口IH』『室内サウナ』『**チェックイン15:00〜18:00**』に対し、
    洲宮は『ガスコンロ×1』『屋外薪バレルサウナ』『**15:00〜24:00**』」

とあり、**CLAUDE.md はこの施設を「サイト内で数値が食い違う」例として挙げてきたが、
実際には姉妹施設の値の混入だった。** 食い違いに気づいたら、その場でDBを直すこと。

**検出の糸口はいずれも外部の独立した住所や文章との突き合わせ。** DB内部の整合性検査では
見つからない。GEOSPOT は3棟が数メートル以内にあるため、**座標との距離では原理的に
区別できなかった。**

### 住所が「運営会社の管理事務所」のことがある（2026-09 に id=185 で判明）

姉妹施設との混線でも誤記でもなく、**同じ会社の別の建物**を指していた型。
公式には両方が載っていて役割が違う。

  「Access アクセス **住所：長野県長野市南長野県町４６６**」
  「Company 運営会社 株式会社ライフ・パートナー …
    **管理事務所 長野市北石堂町１１９１－２０**」（id=185 SAUNA VILLA 然）

DBに入っていたのは**後者**だった。一休も楽天も「南長野県町466」で、
**公式・一休・楽天の3つが一致する。**

**座標が決定的だった。** 保存座標は Google Places の `searchText`（施設名＋住所）で
引いているので、住所が誤っていても施設名が効いて正しい場所に寄る。

  「長野市南長野県町466」をGSIで引いた点   … **0.05 km**
  「長野市北石堂町1191-20」をGSIで引いた点 … **7.41 km**（解決できず「長野市北郷」に落ちる）

**会社概要・特定商取引法の表記に出る住所を施設の住所と取り違えない。**

### 住所に番地が無い施設は住所照合から漏れる（id=198）

DB が「長野県北佐久郡御代田町塩野」で番地を持っておらず、**食い違いようがないので
監査に引っかからなかった。** 楽天が「塩野**482-90**」を出しており、

  「御代田町塩野482-90」をGSIで引いた点 … **0.18 km**
  「御代田町塩野」（大字のみ）        … **3.04 km**

**番地を足すと座標に寄る。** 大字だけの住所は地図が3km離れた場所を指す。
`official` を持たない施設だったので、楽天が唯一の読める出典だった。

### 番地の枝番の食い違いは、この手法では切り分けられない

**国土地理院のジオコーディングは番地までしか解決しない。**
「吉田888-45」も「吉田888-44」も同じ「吉田８８８番地」に落ちるので、
保存座標との距離が**完全に同じ**になる。2026-09 に5件が未決着で残った。

  id=50 不動堂452-**4**／楽天452-**22** ・ id=167 仙石原**474-30**／**480-1** ・
  id=228 吉田888-**45**／**44** ・ id=250 大川1075-**46**／**47** ・
  id=252 池614-**171**／**168**

**住所監査（addr をジオコーディングして座標と比べる）の盲点。** 大字・町名の誤りは
拾えるが、枝番の1桁違いは拾えない。**別の独立した出典（Googleマップの事業者情報、
施設自身の予約エンジン）が要る。**

---

### 公式が機能していなくても、予約ボタンの遷移先に諸元がある

**id=182 The Aurora Chalet** の公式 `jadehotelgroup.com` はリンク先がすべて `/blank-*` の
Wix 未編集プレースホルダーだったが、「BOOK YOUR STAY」から遷移する**運営自身の予約エンジン**に
施設が実在し、住所も一致して部屋タイプ別の定員（8 / 8 / 10 / **20**）まで取れた。

**id=159 葉山THE・TERRACE HOUSE** も公式の `/overview` が2回とも本文の空なページだったが、
「予約する」の遷移先である運営会社の予約ポータルに
「最大人数：13名　部屋数：4　ベッド数：7」があった。**同ポータルが姉妹施設を
25名・9名・10名と出しており、9名上限のようなシステム制約が無いことも確認できた。**

**公式サイトが写真とコピーだけでも、予約導線を1つ先まで追うと諸元表が出てくる。**
chillnn・beds24・airhost・tabichat・reservation.jp などの予約エンジンは、
料金計算のために定員や設備を構造化して持たざるを得ない。

## 情報が取れやすいサイトの型

収量を分けるのは**諸元表の有無**であり、サウナ専用ページの有無ではない。

**当たり**: 「Information」「施設概要」「設備一覧」「よくあるご質問」を持つサイト。
1施設10〜18項目取れる。とくに**注意事項ページ**は制約を書く必要があるため具体的
（禁止事項、時間制限、持参が必要なもの）。**FAQ** は運用ルール系（チェックイン方式、
アーリー/レイト、定員内訳、料金）の宝庫。

**外れ**: イメージ写真とコピー主体のラグジュアリー系。定員すら書いていないことがある。
サウナ専用ページを持ちながら中身が形容詞だけ（THE NALU、STAR VILLAGE、Casita Laguna）。

**商品として売っているものは仕様が書かれる。** 有料オプションや日帰り営業をしている
サウナは、料金表を出すために定員・時間・機材を明示せざるを得ない。

---

## スキーマの不足（12件の取りこぼし）

| 項目 | 不足の内容 | 件数 |
|---|---|---|
| ~~`coldbath`~~ | ~~プール兼用・温泉浴槽兼用の選択肢がない~~ **2026-08 に `pool` / `tub` を追加して解消** | 12 |
| ~~`early_late`~~ | ~~アーリーとレイトが1項目にまとまっている~~ **2026-09 に分割して解消** | 3 |
| ~~（新規）~~ | ~~季節制限を持つ項目がない~~ **2026-09 に `coldbath_season` を追加して解消** | 5 |
| ~~`kitchen_type`~~ | ~~カセットコンロしか無い施設をどう扱うか未決~~ **2026-09 に `cassette` を追加して解消** | 4 |
| `sauna_type` | サンドウォール／マグマスパ／ミスト／トレーラー／遠赤外線 | 9 |
| `coldbath_season` | **夏季だけ水風呂にならない**施設を表現できない（id=27） | 1 |
| ~~`water_depth`~~ | ~~体感基準のみで cm 表記を取り込めない~~ **2026-09 に cm 帯へ置き換えて解消**（1件 → **72件**） | 1 |
| `coldbath` | **海を水風呂にする施設**を表現できない（id=59 Under the Sea UBARA。ikitai の「海が水風呂＝○」。`river` は川・湖で別物） | 1 |
| `water_src` | 雨水を使う水風呂（Kito NASU） | 1 |

**`cassette` は「備え付けが無い」ことが条件。** 2026-09 に対比する2件が出た。

| 施設 | キッチン設備の記載 | 値 |
|---|---|---|
| id=23 The TRAVELERS Chateau | 火口は「カッセットコンロ」だけ | `cassette` |
| id=17 the MELLOW HOUSE | 「カセットコンロ、**３口コンロ**」と併記。３口の熱源は不明 | **入れない** |

併記されていたら備え付けがあるので `cassette` は使えず、かといって ih/gas も
分からないので未調査に戻す。

**`kitchen_type` に `cassette`（カセットコンロのみ）を追加した。** 備え付けの
コンロが無くカセットコンロだけの施設が4件あり、`gas` と書くと
「ガスコンロあり」で絞り込んだ利用者が卓上の携帯コンロだけの施設に当たる。
id=95 の既存値 `gas` を `cassette` に訂正し、判断を保留していた id=211 /
id=24 も記録できるようになった（id=266 は「カセットコンロのレンタル」で
備え付けではないため入れない）。

**`coldbath_season`（通年利用可 / 冬季休止）を追加した。** 冬季に水風呂が
凍結・休止する施設が実際に見つかっており、サウナ目的の利用者には決定的だが
記録する場所が無かった。現在 `winter` 14件 / `year` 5件。

  「※冬季は凍結防止のため屋外の水風呂、シャワー、水栓の使用を休止させて
    頂きます。」（id=103 Private villa FujiNagi）
  「冬季（11月～3月末）は水道凍結の恐れがあるため、水風呂のご利用は
    停止しております。」（id=198 T&A）

SANU の MOSS型5拠点は公式マガジンが名指しで対象拠点を列挙しており、
そこに「テラスの水風呂は冬季11〜4月は凍結防止のため利用制限」とある。
**なお LEVATA の「12〜3月は露天サウナ使用不可」はサウナ本体の制限なので
この項目には当てはまらない。**

**逆向きの季節性を表現できない（id=27 Sumera Resort Minato）。**

  「プール365日利用可(**夏季以外**はサウナの水風呂として利用可)」
  「温水プールではありません。**夏以外はサウナの水風呂としてご利用可能です。**
    毎回水は入れ替え。通年水を貯めております」

水は通年張られており、**秋冬春はむしろ水風呂として機能し、除外されるのは夏だけ。**
この項目は「冬季に凍結・休止するか」を想定しているので当てはまらず、値を入れなかった。

**`early_late` は 2026-09 に `early_checkin` / `late_checkout` へ分割した。**
16件を出典から再判定した結果、**両方可11 / レイトのみ可3 / 両方不可2 /
アーリーのみ可0**。分割の必要性は id=14 Sea by TORAMII が示した。旧値は
`yes` だったが実際は「アーリーチェックインは現在行っておりません。」
「有料でレイトチェックアウトもご用意しております。」で、**`yes` が
アーリー不可を隠していた。**

「レイトは可、アーリーは一切言及なし」の2件（id=78 / id=267）も、
1項目では「可」と「不明」を同時に表現できなかった。

**分割後も残る課題**: 「両方可」11件のうち9件が「有料」「応相談・
利用状況による」の条件付きで、可/不可の2値では条件の有無が失われる。

`coldbath` のプール兼用は最多だったため 2026-08 に選択肢 `pool`（プール兼用）と
`tub`（浴槽・ジャグジー兼用）を追加した。未調査に落ちていた12件を記録でき、
その後も増え続けている（現在 bath 142 / tub 26 / pool 20 / shower 2 / none 1）。

  「長さ8mｘ幅4mのプールが水風呂代わりです」（id=48 → pool）
  「浴室に2つ浴槽があり1つは温泉、もう1つは水風呂、もしくはお湯を
    入れることができます。」（id=144 → bath から tub に訂正）

**この項目で分かったこと: 偏っていても既定値とは限らない。** `coldbath` は
99%が `bath` で偏り検査に引っかかっていたが、原因は逆で、**曖昧な施設を記録せず
未調査に落としていた**ためだった。裏取り済み43件でも42件が `bath` で、実際に
調べても水風呂が見つかっている。

**否定値がほとんど記録されていない。** 公式サイトは「ある物」は宣伝し
「禁止事項」は明記するが「水風呂はありません」とは書かないため。否定値が
使われている項目（`late_arrival` 59% / `firepit` 19% / `pet_ok` 15%）は
いずれも注意事項ページに制約として書かれるものだった。

**2026-08 の時点で「なし」が一度も無かった5項目のうち、2項目が埋まった。**
いずれも**明示的な否定文**が見つかったときだけで、不記載からは入れていない。

| 項目 | 否定値 | 初出の根拠 |
|---|---|---|
| `loyly` | `no` 3 | 公式FAQ（id=9 / id=10 sendouQ、id=57 Refwind） |
| `coldbath` | `none` 1 | 公式FAQ（id=240 Wellリゾート富士） |
| `outdoor_rest` | **0** | — |
| `rest_chair` | **0** | — |
| `wifi` | **0** | — |

同じ時期に、5項目の外でも2つ埋まった。

| 項目 | 否定値 | 初出の根拠 |
|---|---|---|
| `kitchen_type` | `none` 4 | 一休の○✕欄「× キッチンあり」＋公式FAQ「調理設備は無く BBQ は機材貸与制」（id=105 BLANC FUJI） |
| `chiller` | `no` 4 | **ikitai の備考の否定文**「**チラーなどはないので**季節に依って水温は変動します」（id=48）ほか公式3件 |

**`chiller` の id=48 はサウナイキタイから取れた初めての否定値。**
チェック欄の「無し」は既定表示なので根拠にならない（前述）が、
**備考の自由記述にある否定文は別物**として使える。

季節制限は LEVATA（12〜3月は露天サウナ使用不可）、Hygge Chalet（水風呂は冬季凍結で
利用不可）。サウナ目的の利用者には決定的な情報だが記録する場所がない。

### チェックイン時刻は出典で食い違う（2026-09 に全件監査）

楽天の「チェックイン 15:00（最終：22:00）」と DB の `checkin` を188施設で比較すると
**34件が食い違った**（開始時刻3 / チェックアウト2 / 最終時刻29）。
**これは訂正リストではなく、独立した出典が要るフラグのリスト。**

5件を公式・一休まで当たった結果がこれ。

| 施設 | 決着に使った出典 | DB | 楽天 | 誤っていたのは |
|---|---|---|---|---|
| id=19 GIFTHOUSE那古海岸 | 公式 15:00〜18:00 | 24:00 | 24:00 | **両方** |
| id=43 Montevan | 公式 15:00〜20:00 | 24:00 | 20:00 | **DB** |
| id=46 閑閑舎 | 一休 15:00〜23:00 | 15:00 | 16:00 | **楽天** |
| id=68 SURF UP | 公式・一休 OUT 11:00 | 11:00 | 10:00 | **楽天** |
| id=183 Hakuba Amber | 一休 16:00〜22:00 | 一致 | 15:00 | **楽天** |

**楽天が誤り 4/5、DB が誤り 2/5。どちらも単独では信用できない。**

**公式は開始時刻しか書かないことが多い。**

  「チェックイン **15：00～** チェックアウト 11：00」（id=68 SURF UP 公式）
  「チェックイン **15:30 ~** チェックアウト ~ 10:30」（id=168 湯屋やまざくら 公式）
  「チェックイン / アウト **15:00 / 10:00**」（id=261 Earthboat藤原 公式）

**`checkin` の終了時刻は公式にほぼ出てこない。** 一休を持たない施設
（id=44 久留里山荘、id=277 No.12 Kashima）は決着手段が無い。**端から潰す作業ではない。**

### 一休.com の「設備・特徴」欄は否定値が取れる（2026-08 発見）

一休の施設ページ下部にある**○✕形式の列挙**は、スコープが項目と一致する
構造化データなので、否定値の根拠として使える。公式サイトが「無い物」を
書かない問題をここで回避できる。

```
設備・特徴: ○ 一棟貸切 / ○ キッチンあり / × 温泉あり / × プールあり / × ペット可
（独立行）  サウナ  あり／なし
基本情報:   ペット 不可／可、IN 16:00～23:00、OUT 10:00、客室数
```

- 「× サウナ」「サウナ なし」→ `sauna_exists: no` を記録してよい。
  ただし公式にも記載が無いことを確認する。これで id=202/203 を記録した
- **「サウナ あり」だけでは `yes` / `room` / `shared` を区別できない。**
  あり の場合は公式で客室設備か共用設備かを判定する
- 「× ペット可」「ペット 不可」→ `pet_ok: no`。ただし**同じページの基本情報や
  実在するプランと突き合わせること。** SANU 3件（id=120 / 177 / 246）で、
  ○✕欄が「× ペット可」なのに基本情報は「ペット 可」で、しかも
  「愛犬と泊まれる」プランが実際に予約できるという矛盾が出た。
  部屋限定のサービスをアイコン化できていないためとみられる
- 「○ キッチンあり」では ih / gas が分からないので `kitchen_type` は入れない
- **「IN 15:00～23:00 OUT 11:00」は時刻の出典として最も信頼できる。** 2026-09 に
  楽天・公式と突き合わせた3件すべてで DB と一致した。**プラン単位の表示とも一致する**
  （id=68 は全プランが IN 15:00～20:00 / OUT 11:00）。**DB の `checkin` はもともと
  一休由来とみられる。** ただし**プラン別の時間帯**は施設の時間帯と別物
  （id=183 の「【素泊】18時以降のチェックインでお得！レイトチェックインプラン
  IN **18:00～22:00**」）

**一休に載っていない施設には使えない。** 桃乃八庵（id=201）は同ブランドの
2施設が一休の✕で `no` を記録できたのに、自身は一休に施設ページが無いため
未調査のまま残った。楽天の設備一覧にもサウナは無かったが、**それは✕ではなく
単なる不記載なので否定の根拠にならない。**

---

## index.html の意匠（2026-09 に刷新）

**避ける形をあらかじめ決めてある。** 既定の部品を並べると次の顔になり、
これが出たら直す。

均一な余白／均一なカード／均一な角丸／均一なボタン／縦に並べただけのフォーム／
情報の強弱が弱い／全部同じ重要度／Material Design のテンプレート感／
デフォルトコンポーネントの多用。

### 何を捨てたか

| 捨てたもの | 代わりに置いたもの |
|---|---|
| ヒーローのオレンジ全面グラデーション＋浮遊する円3つ＋波形SVG | 明朝の見出しと検索欄を左右非対称に組み、下に罫線区切りの数値4つ |
| 数字のカウントアップ演出 | 最初から確定値を出す（JS が動かない環境で 0 のままにならない副次効果） |
| 上部のオレンジ帯（キャッチコピー）＋PR表記の2段 | PR表記1本。コピーはヒーローと同じことを言っていたので消した |
| 「気分で選ぶ」の等分4列カード | 6列グリッドに大小6枚を組んだモザイク |
| 「エリアから探す」の写真カード | 3列の活字インデックス（県名＋件数＋地名）。**カード一覧が2つ続く**のを避ける |
| 絞り込みの白い角丸カード | 罫線だけの5行。行ごとに操作の手触りを変える（枠付き／下線／塗り／つまみ） |
| カードの白箱・角丸16px・14色のパステルピル・全枚に置く「詳細・予約」ボタン・白丸のハート | 罫線とタイポグラフィだけ。カード全体がリンク |
| グラデーションの浮遊ボタン（気になるリスト） | 画面隅に収まる黒いタブ。**0件のときは出さない** |
| 角丸30pxの「もっと見る」カプセル | 一覧を締める、上下に罫を持つ帯 |
| 診断モーダルの角丸カードを積む作り | 罫線で区切った選択肢の列。右端のシェブロンがホバーで滑る |
| カード／モーダルの「持ち上がる」ホバー（translateY）と scale(.95) の出現 | 写真の寄りと見出しの滑り。出現はわずかな上昇のみ |
| 中央揃えの黒グラデーションのフッター | 左に名義・右に導線の2段組 |

### 効かせている強弱

- **先頭2枚だけ形を変える。** 286枚が同じ重さで並ぶとどれも残らない。
  大きくすると1枚で画面を占めるので、**横組みにして行を占有**させる
  （見出し26px・価格20px・本文3行）。`grid-column:1/-1`。
  2列ぶんだけ占めると3列グリッドの右端に穴が空く
- **初回は26枚読む。** 先頭2枚が行を占有するので、残り24枚なら2/3/4列の
  どれでも最終行が埋まる（`loadMore()` の `take`）
- セクション見出しは 01/02/03 の連番。**本題（ヴィラ一覧）だけ罫を2pxにして題も大きくする**
- オレンジは「数の強調」に限る（掲載施設286・件数・県別件数）
- **カードの諸元行（形式・熱源・室温・定員）はこのサイトにしかない情報**なので、
  桁を揃えたグリッドに組む。タグ行は `·` 区切りの平文にして、
  「小さい灰色の2行」に見えないようにする。
  **縦罫で区切ってはいけない**（狭い幅で折り返すと行末に罫だけが宙に浮く）

### 写真が入るまでの仮置き

`VILLAS` は 286件すべて `img` を持たない（**写真は掲載許可を取得してから入れる**）。
仮置きは施設名の頭文字を組む。灰色の家アイコンを286枚並べるより一覧として読める。

**頭文字は棟名側から採る。** 「古民家宿るうふ 揺之家」のようにブランド名が
先頭に来る施設は、先頭1字だと**同じ札が4枚続いて描画の失敗に見える**。
和名（かな・漢字を含む）で空白があれば最後の区切りの頭を使う。
地色も id で5種に振り分ける。

### 探しやすさの手当て（2026-09 の2回目）

意匠は維持したまま、**286件から選ばせる導線**を足した。

| 追加・変更 | ねらい |
|---|---|
| グローバルヘッダー（`.gnav`）を PR 表記とヒーローの間に固定 | どこまでスクロールしても「ヴィラを探す／エリア／気分／お気に入り」に戻れる。PCは文字リンク4つ、**スマートフォンは検索とハートのアイコンだけ**（ハンバーガーは置かない） |
| ヒーローの検索を枠付きに | 下線だけでは検索欄と気づかれない。虫眼鏡＋枠＋墨色の「探す」で入力欄だと分かるようにした。カード化（影・大きな角丸）はしない |
| クイック検索（サウナ／温泉／ペット／海） | 丸いピルを並べず、**文字＋小さな図＋細い区切り線**。`filterByTag()` にそのまま繋ぐ |
| 「気分で選ぶ」を**写真4枚**に | 体験（気分）と設備・条件を UI で分ける。大人数・BBQ は消さず、下段の「ほかの条件から探す」へ移した（露天風呂・海・湖も追加） |
| 「エリアから探す」を「41軒／地名／→」に | 検索フィルタの一覧ではなく**行き先の一覧**として読ませる。挙動は既存の `filterByPref()` のまま（県を選ぶ→一覧へスクロール） |
| 絞り込みを エリア／こだわり／予算／並び順 の4行に | 初見で5行を同じ重さで見せない |
| サウナの仕様を「サウナ好き向け詳細検索」に畳む | ただの「その他」にすると差別化要素が埋もれる。名前を与えて別の面として開く。**畳んだままでも `n件選択中` を出す**（`applyFilters()` から `syncSaunaMore()` を呼ぶ） |
| **スマートフォンの3列（compact）表示を廃止** | 写真を眺めるギャラリーではなく、施設名・価格・サウナの仕様を見比べる一覧。3列では文字が小さくなりすぎる。切替UI（`.view-toggle`）ごと外した。`setGridView()` は残してあるが `.compact` の指定が無いので効かない |

**ヒーローの見出しは桁あふれしていた。** `word-break:keep-all` で折り返さないため、
2行目「とっておきのヴィラへ。」が左の列（1440px で 684px）を超えて右へ流れる。
検索欄が背低だった間は下に抜けていて見えなかったが、**問いかけ＋クイック検索で
背が高くなった瞬間に重なった。** 列の取り分（`1.75fr / minmax(300px,1fr)`）と
上限（`clamp(29px,4.65vw,56px)`）で合わせ、**1段組みに切り替える幅も 880px →
1000px に上げた**（880px だと左の列が狭くなりすぎて同じ衝突が起きる）。

**固定ヘッダーぶんのずれは `.section{scroll-margin-top:66px}` で吸収する。**
`filterByTag` / `filterByPref` / `heroGo` / `showFavs` / `lucky` / `finishQuiz` が
すでに `scrollIntoView({behavior:'smooth'})` を持っているので、**JS 側は1行も
書き換えずに済む。**

**キャッシュ対策は 2026-09 に恒久化した**（下の「共有アセットのキャッシュ」を見よ）。
それまでは `?v=<時刻>` を手で付けていたが、リンク先の CSS/JS には効かず
「直したのに反映されない」を何度も起こした。

## villas/*.html の意匠（2026-09 に刷新）

**286ファイルのインライン CSS は完全に同一。** だから `<style>` ブロックを丸ごと
差し替える1回の置換が286件すべてに効く。**変更後は必ず「CSSの種類が1つのまま」を
確かめること**（ズレると以後の一括置換が効かなくなる）。

index.html と同じ組に揃えた。捨てたもの：オレンジのグラデーションのヘッダー／
18px・30px・14px・12px の角丸／10色のパステルピル（`style=` で直書き）／
9色の県バッジ／淡いオレンジの囲み（`.modal-feat`）／等分の白いカード（`.fact`）／
影と持ち上がりのある OTA ボタン／カプセル型の公式・SNS リンク／
家アイコンの仮置き（施設名の頭文字に）。

**構造も1つ変えた。** `.spec-block`（サウナの仕様）は `.map-block` の中にあり、
**狭い左の列に押し込まれていた**。このサイトの差別化要素なので、grid-area を
`"hero right" "map right" "spec spec" "back back"` にして**2段の下に全幅**で出す。
広い幅では3列に組む。

**`position:sticky` はグリッドの行を抜ける。** 地図に付けていた sticky が、
下に置いた全幅のスペック表に重なった（Chrome で確認）。仕様を全幅に出した以上
貼り付ける必要もないので外した。

**個別ページには PR 表記が無かった。** アフィリエイトの OTA リンクを並べている
のに掲載表記が1文字も無く、index.html にしか無かった。上部の帯とフッターの
両方に入れた。

**仮置きの頭文字は「末尾の語」をそのまま採ってはいけない。**
「古民家宿るうふ 揺之家」は末尾を採って `揺` が正しいが、
**「Haga Farm＆Glamping（芳賀ファーム&グランピング）」は `F` になってしまう。**
**末尾の語が和字で始まるときだけ**末尾を採り、それ以外は先頭を使う。
21件が直った（`郷音 -G.O.A.T.-` が C、`軽井沢 HOUSE VILLA` が V など）。
**index.html の `cardHTML()` も同じ規則に揃えてあるので、片方だけ直さないこと。**

### 組み直し（2026-09 の2回目）

**1回目は色と角丸を直しただけで、組みは旧いままだった。** 写真の右に本文を全部
積む作りで、見出し・紹介文・諸元・料金・予約・リンクが同じ重さで縦に並び、
「全部同じ重要度」になっていた。`.layout` を組み直した。

```
旧: [写真][本文ぜんぶ] / [地図＋仕様] / [戻る]
新: [見出し（全幅）] / [写真][料金と予約] / [紹介文] / [仕様] / [地図] / [口コミ] / [戻る]
```

- **施設名をページで最大の文字にする**（45px）。料金は37px。読む順に大きさが下がる
- 見出しの上に「県 ─ 市」を出す。市は `modal-addr` から正規表現で取る
- **決める側（料金・予約）は罫1本で読む側から分ける。** 囲みは作らない
- `grid` の `row-gap` は 0 にして、**行の間隔は各ブロックの margin で個別に取る**
  （見出し→写真26／写真→紹介文30／紹介文→仕様66／仕様→地図58）。
  gap で等分にすると区切りの重さが全部同じになる
- **予約サイトは同じ色の四角を並べない。** 各社の色は左の3px罫だけに使い、
  行の一覧に組む。押すと行が各社の色で塗りつぶれる
- **諸元は等分3列をやめ、定員だけ27pxの数字**にして IN/OUT を右に小さく積む
- 地図は住所を左・地図を右に置く（同じ大きさの箱を2つ縦に重ねない）

**レビューで出した「AIっぽい」4点と直した内容。**

| 出た点 | 直した内容 |
|---|---|
| 注記（※掲載情報は…）が料金と予約導線の**間に割り込んで**いた | パネルの末尾へ移した |
| **いちばん大事な「空室・料金を探す」がパネルでいちばん小さかった**（10.5px muted） | 12.5px・墨色・太字に。強弱が逆だった |
| 公式サイトと SNS 3つが**全部同じ重さ**（均一） | 公式は13px・実線の下線、SNS は10.5px・図中心に落とす |
| **「一覧に戻る」が口コミより前**に出ていた | 口コミ→戻る の順に |

**`spec.js` の出力順は SCHEMA 順なので、先頭＝サウナ本体の仕様。**
23項目を同じ大きさで並べると全部同じ重要度に見えるので、**先頭の1行だけ**
19px＋オレンジのラベルにする。**列数が変わると「先頭の1行」の件数も変わる**ので、
`nth-child(-n+2)` / `nth-child(3)` / `nth-child(2)` を幅ごとに切り替える。
**列数の指定は `spec.css` 側に置く**（ページ側に書くと CSS 差し替えで落ちる。実際に落とした）。

**`.review-widget` は285件で `.wrap` の外にあり、余白なしで全幅に出ていた。**
`.wrap` の中へ揃えた。1件（id=0）だけ中にあり、**書式が2通り混在していた。**

**`fix_villa.py` を1つ壊していた。** `add_tags` の pill 雛形を
`<span class="pill" style="[^"]*">` で探しており、**パステル10色の inline style を
外した時点で雛形が0件になっていた**（エラーではなく「雛形が見つかりません」と
出て黙って飛ばす）。`style` の有無を問わない形に直した。
**個別ページの形を変えたら `fix_villa.py` と `validate.py` の正規表現を必ず通すこと。**
依存しているのは3つ。

```
<span class="fact-label">定員</span><span class="fact-val">…</span>   fix_villa
<div class="modal-addr">住所<a …   ← 実際は住所の直後に <a。「住所」の語は
                                      別の .modal-addr-label（所在地）    validate
<a class="ota-btn" …>楽天トラベル<span>…                              validate
```

### 施設スペックの組み（2026-09 に作り直した）

**`buildHTML()` は SCHEMA の群を捨てていた。** 調査済みの値を群をまたいで1つの
配列に集め、平らな格子に並べていたので、**どれがサウナの話でどれが料金の話か
読み取れなかった**（未調査のほうは群ごとに出していたのに、である）。

```
旧: [23項目を3列の格子に平らに並べる] ＋ 全項目に出典の札
新: [サウナ＝全幅の帯] ＋ [残りの群を2段に流す] ＋ 出典は群の見出しに1回
```

- **群の中で出典が1種類なら、印は見出しに1回だけ出す。**
  同じ「公式」の札が23個並ぶと情報ではなく模様になる。
  出典が割れている群だけ、値の脇に個別の印を付ける。
  **実測で 27個 → 4個**（id=0）
- **先頭の群（サウナ）だけ全幅の帯**にして、ラベル上・値下で数字として立てる。
  ただし**3項目以上あるときだけ**。1〜2項目だと帯が全幅に伸びてセルが1つ残り、
  間延びする（id=148 がこれ）
- 残りの群は `columns:2` で流す。**群ごとに項目数が違うので高さが自然に不揃いに
  なり、同じ札の反復にならない**
- **進捗バーをやめ、「どこまで引かれたか分かる罫」にした。** 見出しの下の線の
  長さがそのまま調査済みの割合。色は墨にして部品然とさせない
- **未調査リストも2段に流す。** 全幅1列だと項目名と「未調査」が1000px離れて
  対応が読めなかった。**群の区切りに罫は引かない**（段の先頭に罫だけが残る）

**折り返しは実測で潰す。** 「セルフロウリュ可」が帯で2行に折れ、
「アーリーチェックイン」がラベル欄で2行に折れていた。帯の最小列幅を
152→168px、ラベル欄を 7.6→10.6em にして解消（`getBoundingClientRect().height`
が `line-height` の1.45倍を超える要素を数えて0件を確認）。

**`spec.js` は ES5 のまま保つこと。** CLAUDE.md 冒頭の制約で、`spec.js` 冒頭にも
同じ記載がある。書き換え後に アロー関数／テンプレートリテラル／const／let が
0件であることを確認した。

**`renderValue()` から出典の札を分離した**（`srcBadge()` を新設）。
群の見出しへ札をまとめるため。**`window.villafarasSpecHTML` /
`villafarasSpecRender` / `VILLAFARAS_SPEC_SCHEMA` / `_OPTIONS` / `_RANGE` は
そのまま。** `spec-survey.html` と `tools/validate.py` / `blanks.py` が使うので
消さないこと（変更後に30問の描画と両ツールの動作を確認済み）。

**`spec.css` は villas/*.html 専用**（index.html は読み込まない）。
**列数の指定は spec.css 側に置く。** ページ側の `<style>` に書くと、
ページCSSの一括差し替えで落ちる（実際に3列指定を落とした）。

**`spec.css` と `reviews.css` も個別ページに出る。** 角丸カード・4色の出典バッジ・
塗りのボタン・縦積みのフォームを直した。**`reviews.css` の
「サ・スペック表」（`.sauna-spec-table`）は未使用のうえ、`.spec-row` / `.spec-k` /
`.spec-v` が `spec.css` と名前で衝突していた**ので削除した。
**`.card-rating` 系は index.html の一覧カードでも使うので触っていない。**

**リンク先の CSS/JS は HTML のクエリでは外れない**（下の「共有アセットの
キャッシュ」で恒久対応した）。

---

### 直せていないもの

- **`index.html` の JS が ES6 のまま。** CLAUDE.md 冒頭の「ES5構文のみ」に反する。
  刷新前から全体がテンプレートリテラルと `const` で書かれている。**違反はこの1
  ファイルだけ**で、villas/*.html 286枚・privacy / contact / owner / spec-survey・
  `spec.js` / `villa.js` / `reviews.js` はすべて ES5（2026-09 に全件走査して確認）。
  **古い端末でもトップページ以外は全部動く。**
- ~~**プライバシー／お問い合わせのモーダルが死んでいる**~~ — **2026-09 に削除**（下記）

### 死んだコードが古い端末でページ全体を殺していた（2026-09）

**片付けの話ではない。** 施設詳細が `villas/*.html` に移ったあとの残骸のうち1行が、
呼ばれていないのにトップページを壊していた。

```js
index.html:1092   return (SNS_ORDER[ka]??99)-(SNS_ORDER[kb]??99);
```

`??` は **ES2020（Safari 13.1 / 2020年3月）**。しかもこれを含む `linksRowHTML()` は
**呼び出し0の死んだ関数**だった。**構文エラーは呼ばれなくても起きる。**
JSはインライン `<script>` 1つ（265,586文字）に全部入っているので、解析に失敗すると
**ブロックごと捨てられる。** 286枚のカード・検索・絞り込み・お気に入り・診断が
すべて出ず、**見出しと検索窓だけが残って下が白くなる。**

| | 互換性の下限 |
|---|---|
| 削除前 | **iOS 13.4**（2020） |
| 削除後 | **iOS 10**（2016） |
| 完全な ES5 化をした場合 | iOS 9.3（2015） |

**実際に壊れていて救われたのは iPad Air・mini 2/3**（上限 iOS 12.5.7）。
完全な ES5 化で追加で救えるのは iPad 2/3/mini 1（2011-2012年）だけなので、
**アロー29・テンプレートリテラル32・const 48・let 6 の書き換えは保留にした。**
検索・お気に入り・診断・並び替えを 265KB ぶん書き換える価値があるかは、
対象の実機が特定できてから判断する。

消したもの（計165行）。

  OTAアフィリエイト設定のコメント＋`OTA_META` / `OTA_ORDER` /
    `OTA_FALLBACK` / `otaButtonsHTML` / `SNS_ICONS` / `snsIconKey` /
    `snsLabel` / `SNS_COLORS` / `SNS_ORDER` / `linksRowHTML`      79行
  プライバシー／お問い合わせのモーダル（HTML）                     68行
  `openPrivacy` / `closePrivacy` / `openContact` /
    `closeContact` / `submitContact`                              13行
  `setGridView`（`.compact` のCSS指定が無く切替UIも撤去済み）       5行

**Escape のハンドラが消した関数を呼んでいた。** 消しただけでは Escape を
押すたびに ReferenceError が出るところだった。**死んだコードを消すときは、
定義側の参照数だけでなく「呼んでいる側」を必ず grep すること。**

```
- if(e.key==='Escape'){closeQuiz();closePrivacy();closeContact();}
+ if(e.key==='Escape'){closeQuiz();}
```

**アフィリエイトの差し替え先が変わった。** 消したコメントは「各 url の中身を
あなたのアフィリエイト用リンクに差し替えてください」と `OTA_META` を指していたが、
**その `OTA_META` はもう誰も読んでいない。** 実際の予約リンクは DB の `ota` を
`villas/*.html` に描いたもので、**差し替えるなら `tools/fix_villa.py` の
`set_villa` / `remove_ota`。**

**`index.html` にあった `info@villafaras.jp` 3件は全部このモーダルの中だった。**
トップページには元から生きたメールアドレスが無く、生きているのは
`contact.html`（1件）と `privacy.html`（2件）だけ。

検証は `node --check` と、**変更前後のページを並べた描画の比較**で行った
（カード26枚・先頭5件・ヒーローの数値 286/255/80/120・県チップ9件・
ペット絞り込みの結果がすべて同一、コンソールのエラー0）。

---

## サウナ情報を主役にする（2026-09。個別ページの第3段）

**286ページの HTML にサウナ情報を書かない。** 追加は `spec-data.js` を直すだけで
全ページに出る。個別ページは差し込み口（空の div ＋ `data-villa-id`）だけを持つ。

```
spec-data.js ─┐
data/villas-lite.js ─┤→ villa.js  → SAUNA AT A GLANCE / 体験動線 / 似たヴィラ / 固定CTA
spec.js（SCHEMA）  ─┘→ spec.js   → SAUNA と STAY の2区分の諸元表
```

- **`data/villas-lite.js`**（`tools/make_villa_lite.py` で生成）。個別ページは
  index.html の `VILLAS`（234KB）を読めないので、id・名前・スラッグ・県・タグ・
  価格だけを 46KB に出す。**index.html の VILLAS を触ったら再生成すること。**
- **`villa.js`** は ES5。差し込み口は `.sauna-glance` / `.sauna-flow` /
  `.similar-villas`。**値が無ければ div ごと消す**ので、情報の少ない施設で
  「不明」が並ばない（id=148 は要約も動線も出ない）
- **SCHEMA に `sec:'sauna'|'stay'` を足し、「水風呂・外気浴」を2群に割った。**
  体験の順（サウナ→水風呂→外気浴）で読ませるため。キーは変えていない
- **サウナが無い施設に SAUNA の節を立てない。**「サウナ なし」という事実は
  消さず、宿の詳細の側に寄せる（`sauna_exists==='no'` で判定）
- **「○/○ 調査済み」と「未調査の項目を見る」を一般UIから外した。**
  未確認項目は非表示。調査率は `.spec-meta` の data 属性に残してある。
  出典は群の見出しに1回＋「情報の確認方法 ⓘ」に集約
- **似たヴィラは既存データの一致だけで点を付ける**（県3点／熱源3点／形式2点／
  ロウリュ2点／共通タグ1点ずつ／近い価格1点）。5点以上を上位3件。推薦のために
  値を作らない

**`position:sticky` をグリッドの行に付けると抜ける（2度目）。** 料金パネルを
`.buy-block` に sticky で付けたら下の節に重なった。**内側を `.buy-inner` で
包んでそこに付ける**（親の高さが上限になる）。

### 手で書いた件数はずれる

ヒーローの数値とエリアの件数を HTML に直書きしていて、**実測と食い違っていた。**

| 表示 | 実際 |
|---|---|
| 長野 28 | **27** |
| 群馬 11 | **12** |
| サウナ付き 278 | **255**（`sauna` タグ） |
| 温泉付き 81 | **80** |
| ペット可 119 | **120** |

**VILLAS から数え直すようにした。** 以後ずれない。同じ理由で、サウナが無い
5施設のタイトルが「〜のサウナ・温泉付き一棟貸しヴィラ」と言い切っていたのも直した
（id=2 / 183 / 202 / 203 / 229）。

**残る281件も同じ定型で、2026-09 に `tools/fix_titles.py` で作り直した（219件変更）。**
手で書いた定型が事実とずれる、という点で件数の直書きとまったく同じ事故だった。

  温泉が無いのに「温泉付き」            **203件**（温泉タグを持つのは80件）
  客室サウナが無いのに「サウナ付き」    **26件**（共用18・未調査8）

**未調査8件がいちばんまずい。** 調べていない施設に「サウナ付き」と書いており、
公開情報で確認できた値だけを出すという方針に正面から反していた。

出し分けは `sauna_exists` と `onsen` タグから導く。判定基準どおり yes/room だけが
客室サウナ、`shared` は「共用サウナ」と書き分け、**未調査は何も書かない。**

| 題の型 | 件数 | 条件 |
|---|---|---|
| サウナ付き一棟貸しヴィラ | 191 | yes/room・温泉なし |
| サウナ・温泉付き一棟貸しヴィラ | 64 | yes/room・温泉あり |
| 共用サウナ付き一棟貸しヴィラ | 12 | shared・温泉なし |
| 温泉付き一棟貸しヴィラ | 10 | サウナなし／未調査・温泉あり |
| 共用サウナ・温泉付き一棟貸しヴィラ | 6 | shared・温泉あり |
| 一棟貸しヴィラ・貸別荘 | 3 | どちらも書けない |

**内訳の合計が元データと一致することを確かめる**（255 = yes 227 + room 28、
shared 18、13 = no 5 + 未調査 8、温泉80）。ここがずれたら出し分けが壊れている。

**施設名は HTML エスケープして照合する。** `&` だけを見ていたときは8件、
アポストロフィ（`&#x27;`）を足して2件が「施設名が題と合わない」で落ちた
（id=96 `yl&amp;Co.Hotel`、id=13 `Ocean&#x27;s Terrace TORAMII`）。
**名前に ｜ を含む施設があるので末尾から切らない**（id=129
「和モダングランピング｜NAGOMI CAMP」）。

**`og:title` と `<h1>` と JSON-LD は元から汚染されていなかった。** 誤りは
`<title>` だけに閉じていたので1行の置換で済んだ。`description` に「温泉」が
出る4件は中身を見て全件そのままにした。3件は地名（那須温泉郷）か
「温泉気分を味わえる檜風呂」で誤りではない。**残る id=216 マイグレ天だけは
「石造りの温泉露天風呂を完備」と書いておりタグと食い違う**が、出典を当たって
いないので値もタグも触っていない（未決着）。

### 価格の軸が表示と絞り込みで違う

**カードと個別ページは `pricePerPerson`（1人あたり）を出し、絞り込みは `pnum`
（一棟あたり）で効く。** 286件すべてで `pnum ≈ price`（一棟）、
`pricePerPerson` とは1件も一致しない。**データは変えず、絞り込み側に
「予算 / 一棟」「〜N円 / 一棟」と単位を明示した。**

### SEO

`index.html` の JSON-LD だけ `https://villafaras.jp/` を向いていた（canonical と
og:url は `alfaras-t.github.io`）。**villafaras.jp は DNS が解決しない**ので
GitHub Pages 側に統一した。個別ページには JSON-LD の `BreadcrumbList` を足し、
パンくずの県は `index.html?pref=<県>` に飛ぶ（index 側に受け口を作った）。
**エリア別の静的ページを作るときは、このクエリをそのままパスに移せばよい。**

## privacy / contact / owner の3ページ（2026-09 に統一）

**3ページとも別々の `<style>` を持ち、ヘッダーもフッターも別物だった。**
index.html と villas/*.html と同じ組に揃えた。**CSSは3ファイルで完全に同一**
（ハッシュで確認する。ずれると以後の一括置換が効かなくなる）。

- ヘッダーは `header.gnav`（ロゴ＋「← トップに戻る」）、フッターは墨地の `foot-in`
- 見出しは `page-eyebrow` → `page-title` → `page-lead` の順に落とす
- **PR表記（topbar）は入れない。** この3ページにアフィリエイトリンクは無い。
  掲載表記は「その面に広告があるから」出すもので、全ページに貼るものではない
- プライバシーポリシーの節番号は **CSS の `counter` で振る**。
  本文から「1.」等を外してあるので、節を足しても番号がずれない
- フォームは villas/*.html の口コミ欄と同じ `.field` / `.field-row`。
  **お問い合わせが縦積みだったので、名前とメールを横に並べた**
- `<select>` は既定の見た目をやめ、`.field-sel::after` の山形に差し替え
- **本文が短いページでフッターの下にクリーム地が残る**ので、
  `body{min-height:100vh;display:flex;flex-direction:column}` で底に貼る

**`contact.html` の送信は `mailto:` を開くだけ**（`submitContact()`。IDで値を拾うので
クラス名を変えても壊れない）。**`owner.html` の formsubmit 先は
`owner-request@example.com` のままで、送っても誰にも届かない。**

## spec-survey.html（オーナー調査票・2026-09 作成）

設計書ステップ5。**オーナーにメールでURLを送り、その施設で空欄の項目だけを聞く。**

```
spec-survey.html?v=123      施設idを付けて送る
spec-survey.html            v が無い／不明なら施設の選択欄を出す
```

**読み込むもの**（順序が大事。`spec.js` が `window.VILLAFARAS_SPEC` を見るため）

```html
<script src="data/survey-villas.js"></script>   施設名と住所だけ（25KB・自動生成）
<script src="spec-data.js"></script>            現在の値
<script src="spec.js"></script>                 SCHEMA / O / RANGE
```

### 分量は「重要12問＋残りは任意」にした

**空欄だけに絞っても中央値33問**（最小20・最大42）になる。設計書は
「一発勝負。2回目の依頼は回答率が激減する」としているので、**12問を先に出し、
残りは折りたたみの任意欄**に置いた。**途中送信を受け付ける**ので、
12問だけ答えて送っても成立し、2回目の依頼が要らない。

重要12問（`CORE` 配列。並び順がそのまま画面の順）

  `stove` `sauna_type` `loyly` `kitchen_type` … **アグリゲーターが持たない4項目**
  `heat_time` `neighbor_dist` … **1件も取れていない項目**
  `sauna_hours` `chiller` `water_temp` `villa_type` `firepit` `late_arrival`

### マスタは3つとも spec.js から受け取る。写さない

`window.VILLAFARAS_SPEC_SCHEMA` / `VILLAFARAS_SPEC_OPTIONS`（`var O`）/
`VILLAFARAS_SPEC_RANGE`（`var RANGE`）。**2026-08 に調査指示へ選択肢を手で写して
`loyly` の `auto` を落とした**ので、マスタは1箇所から配る。

**`var RANGE` は 2026-09 に spec.js へ移した。** それまで `tools/validate.py` に
直接書いてあったが、**調査票も同じ範囲で入力を弾く必要がある**（弾かないと
オーナーの回答が反映の直前で検証に落ちる）。validate.py は `load_range()` で
spec.js から読む。**3箇所に同じ数字を置くと必ずずれる。**

### 受け口は Firestore。**ルールの設定が未了で、送信は未検証**

`specSubmissions` コレクションに POST する（`reviews` と同じ REST + XHR の形）。
**設計書ステップ2が未実装のため、このコレクションのルールがまだ無い。**
Firebase コンソールに次を足すまで送信は 403 になる。

```
match /specSubmissions/{doc} {
  allow create: if request.resource.data.approved == false
                && request.resource.data.villaId is string
                && request.resource.data.answered is int;
  allow read, update, delete: if false;
}
```

`reviews` と同じく **`approved: false` を強制**し、公開読み取りは許さない
（回答には返信先メールアドレスが入りうるため、`reviews` より厳しく読み取り不可にする）。
**本番のFirestoreへテスト書き込みはしていない。**

### 意匠は「フォーム」ではなく「校正刷り」（2026-09 に作り直した）

初版は**設問ごとに同じ白いカードを積む**作りで、29個の同じ箱が並び、
重要12問と任意21問が同じ重さに見えていた。角丸も余白もボタンも均一で、
**どの画面にも流用できる代わりに、この仕事のための画面になっていなかった。**

作り直しの筋は「施設ごとの記録票が刷り上がっていて、空欄だけが残っている」。

- **カードを積まない。** 面は「群」に対して1つだけ与え、設問は**その中の行**。
  設問ごとに箱を作ると強弱が死ぬ
- **角丸は装飾に使わない。** 面2px / 入力3px / 目盛り0 と**役割で変える**
- **余白を等間隔にしない。** 4/7/12/20/34/56/92 の不等間隔で、
  行の上を厚く下を薄く取る
- **操作面を設問の型で変える。** ここが最も効いた。

  | 型 | 形 | 理由 |
  |---|---|---|
  | 二択 | 枠を共有した帯（セグメント） | 対立する2値であることを形で示す |
  | **順序のある選択肢** | **横に伸びる目盛り** | `heattime` `wtemp` `depth` `ndist` `late` は順序尺度。バラバラのチップで出すと「並びに意味がない」という誤った合図になる |
  | それ以外の多択 | 破線の下線。選ぶと実線 | 枠付きの箱にすると二択と見分けがつかない |
  | 数値 | 桁数ぶんの下線＋単位 | 全幅の入力欄にしない |

  **選択で `font-weight` を上げてはいけない**（目盛りの幅が動いて位置がずれる）。
  色と目盛りの背丈だけで示す。
- **重要12問と任意21問で面の性格を変える。** 重要＝白い紙に枠。
  任意＝**枠も地色も与えず罫線だけ**で組み、脚注として沈める
- **進捗はバーではなく12個の目盛り。** どれが済んだか分かる
- 数値欄の下線は**入力と単位までに留める。** 範囲の注記まで含めると、
  空のとき長い罫だけが浮いて「壊れている」ように見えた
- 設問が1つだけの群は詰める（`.grp.solo`）。3問の群と同じ厚みだと1行が浮く。
  **判定に `class="q` を数えてはいけない**（`q-no` `q-head` `q-l` にも当たる）。
  1設問に1つだけ出る `data-k="` で数える

### そのほかの作り

- **下書きは端末の localStorage に保存**（`vf_survey_<id>`）。送信成功で消す。
  `try/catch` で囲ってあるのでプライベートブラウズでも壊れない
- **「現在の掲載内容」を読み取り専用で併記**し、誤りは自由記述欄で受ける。
  全項目を確認させると分量が倍になるため、訂正は1つの欄にまとめた
- 必須項目は無い。**1問も答えず補足欄も空のときだけ**送信を止める

---

## DB 構造の限界

**1エントリ＝1施設**という構造で表現できないケースが繰り返し出ている。

- 複数棟で定員・設備が異なり代表値を決められない（清芳山荘、THE SECOND、
  ドワーフの村、軽井沢HOUSE VILLA、FARM SIDE など6件以上）
- ~~共用温浴施設と客室サウナが併存する（No.12 Kashima Fan Zone）~~ **`sauna_exists` に
  関しては限界ではなかった。「全部屋個室サウナは付いております」と明記されており、
  共用施設が併存していても客室にサウナがあれば `yes`。2026-09 に shared から訂正した**
- 性別で設備が異なる（Nenn: 男性ドライサウナ／女性ミストサウナ）
- **1施設に仕様の違うサウナ室が2つある**（id=221 マイグレYEBISU・id=222 海の声。ドライ＝電気 100℃/110℃ ＋ ミスト＝**ガス** 45℃/47℃。`stove` も `sauna_type` もドライ側しか表せない）。id=99 ハンズアウトドアリゾートも ikitai 上はバレルとテントの2室
- **3室以上になると温度すら代表値を置けない。** id=277 は4室で、共用2室に加えて
  **宿泊者専用の2室が70℃と90℃に割れている**（「宿泊者専用の値」すら決まらない）。
  id=281 は男女各4の計8室。**どちらも全項目を見送った**

現状は「棟で異なる項目は入れない」で対処しているが、`sauna_type` などが
永久に埋まらない。将来的にエントリ分割か棟別データ構造の検討が要る。

---

## 現在の進捗（2026-09-14 時点・サウナイキタイと楽天の全件走査の完了後）

| | 着手時 | 前回 | ikitai走査前 | ikitai後 | 現在 |
|---|---|---|---|---|---|
| 総フィールド | 2745 | 3975 | 4598 | 5191 | **5423** |
| desk10項目 | 34.2% | 52.7% | 65.4% | 72.6% | **73.2%** |
| desk値の出典なし率 | – | 22% | 5.2% | 3.0% | **2.8%** |
| winter_access | – | 3 | 233 | 233 | 233 |
| サウナ yes/room/shared/no/未設定 | 273/4/1/0/8 | 226/27/18/7/8 | 226/29/18/3/10 | 227/28/18/5/8 | **227/28/18/5/8** |

チャネル別の充足は次のとおり（`spec.js` の `ch:` で集計）。

| チャネル | 項目数 | 充足 | ikitai走査前 |
|---|---|---|---|
| A（auto・座標からの導出） | 7 | **97.4%** | 97.4% |
| B（desk・机上調査） | 11 | **67.2%** | 59.8% |
| C（owner・オーナー調査） | 34 | **14.0%** | 7.9% |
| D（review・宿泊者レビュー） | 5 | 0% | 0% |

**チャネルCが2度動いた。** サウナイキタイで `sauna_cap` `sauna_temp` `rest_chair`
`water_temp` `water_depth` `chiller` の6項目が、楽天で `bring_towel` `bring_amenity`
の2項目が埋まった。**設計書がオーナー調査に置いた項目が、机上で埋まることがある。**

**チャネルBの11項目は `coldbath_season` を含む。** 2026-09 に新設したばかりで充足7%
（19/286）なので全体を押し下げている。従来の10項目だけなら 73.2%。

`no` が7件から3件に減ったのは後退ではなく、**根拠のない否定を消した結果**（その後5件）。
2026-08 に7件すべての出典を確認し、3件（resolstay の肯定列挙ページの
不記載を根拠にしていた）を未調査に戻し、1件（id=229）は room に訂正した。
残る3件はいずれも明示的な否定に裏打ちされている。

  公式「テントサウナにつきまして、2026年1月16日をもってご利用を終了
  いたしました。」（id=2）／ 一休の○✕欄「× サウナ」（id=202, id=203）

2026-09 に2件増えて5件になった。**どちらも「サウナがある」と思い込んでいた側の誤り。**

  一休の「宿の施設・設備」欄「**サウナ　なし**」（id=183 Hakuba Amber Resort）
  公式FAQ「**WEAZER Villaのお部屋にはサウナはございません。**」（id=229）

**id=183 は同一住所に3物件が並ぶブランドでの取り違えだった。** 面積で照合すると
DBが指しているのはサウナの無い物件で、サウナがあるのは265㎡・330㎡の別物件。
**住所が一致していても建物が違うことがある。**

**id=229 は「1施設が複数エントリに分かれている」ケース。** DBは Villa（id=229）と
廻（id=230）を分けているが、一休は1施設として扱う。2026-08 に一休を根拠に no から room へ
訂正したが、**エントリが分かれている以上 id=229 が指すのは Villa 単体**なので誤りだった。
**OTA の施設単位の記載を、分割されたエントリにそのまま当てはめると誤る。**

未設定が10件から8件に減ったのは id=69 primera villa と id=64 VILLA Seamu を
一休の肯定記述で `yes` にしたため。**どちらも「公式の不記載」を根拠に一度消していた値**で、
不記載は否定の根拠にならないという規約に戻して復活させた。

「机上調査N施設」という指標は `tools/backfill_src.py` で出典を埋め戻した結果
定義が変わってしまったため、上表では「desk値のうち出典URLがある割合」に置き換えた。

**オーナー調査で聞く想定だった項目の到達状況**

**上位8項目がすべて2026-09 の2つの走査で動いた**（矢印の左が走査前）。
サウナ関連5項目はサウナイキタイ、持ち物2項目は楽天から。

`bring_amenity` **201**←80 / `bring_towel` **172**←77 / `sauna_cap` **128**←46 /
`sauna_temp` **118**←26 / `rest_chair` **94**←37 / `water_temp` **85**←11 /
`villa_type` 77 / `water_depth` **72**←1 / `bring_seasoning` 49 / `sauna_hours` 40 /
`firepit` 39 / `fee_bbq` 37 / `bbq_roof` 27 / `kitchen_burners` 26 / `steps` 26 /
`bring_wrap` 24 / `water_src` 21 / `late_arrival` 21 / `checkin_method` 19 /
`firewood_fee` 16 / `late_checkout` 15 / `chiller` **14**←11 / `early_checkin` 14 /
`comfort_cap` 10 / `kids_free` 8 / `bring_trash` 5 / `sound_rule` 1 /
`fee_pet` 1 / `fee_cleaning` 1

**まだ1件も取れていない項目が5つある。** `neighbor_dist` / `heat_time` /
`fee_person` / `fee_heating` / `bbq_cleanup`。いずれも公式サイトに書かれる
慣習が無く、オーナー調査を待つしかない。

**「オーナーに聞くしか手段がない」という見立ては、情報源を1つ見落としていた。**
当初そう分類したチラー・水温・BBQの屋根に加え、**サウナ定員・温度・水深・休憩イスまで
机上で埋まった。** `water_depth` は 2026-08 の時点で124施設調べて1件（id=143）しか
取れず「実質オーナー調査待ち」と書いていたが、**cm帯へのスキーマ変更と全国走査で72件**
になった。**取れなかったのは項目の性質ではなく、当たっていた情報源の問題だった。**

残る34項目の多くは同じ構造をしている可能性がある。**「公式が書かない」は
「どこにも書かれていない」ではない。**

## 次にやること

1. ~~**`capacity=9` の調査**~~ — **2026-09 に 64件 → 出典なし0件で完了。**
   最後の3件は id=31（9で正しい。プラン名「【愛犬同伴無し/最大9名】」と施設自身の
   予約エンジンが一致）、id=136（9で正しい。SHIRO SUITE 9名／OKU SUITE 7名の棟別最大）、
   **id=182 は 9 が誤りで 20 が正しかった**（一休に最大構成 7BR が載っていなかった）。
   **9 以外の誤りのほうが多かった**（id=6 は 5、id=32 は 13、id=191 は 13、
   id=204 は 8、id=248 は 6、id=283 は 7、id=182 は 20）。

2. ~~**index.html と spec-data.js の capacity 不一致**~~ — **2026-09 に 0 件になった。**
   最後の id=226 マイグレアトリエは spec 側が公式で裏取り済みの 6、index 側が 5 のまま
   取り残されていた。`set_villa` が3箇所を同時に書き換えるので以後はずれない。
   **`validate.py` に不変条件として検査を入れた。**
3. **チャネルBの継続** — 残り空欄1033（`python3 tools/blanks.py --include-done` で確認）。

   **「収量の天井が見えている」という 2026-08 の見立ては外れた。** 当時は
   「公式・FAQ・注意事項・OTA まで開いても埋まるのは2割、20波で +200〜250項目、
   充足 68% で頭打ち」と書いていたが、**サウナイキタイで +593項目、楽天で +233項目**
   入り、チャネルBは 59.8% → 67.2%、desk10項目は 73.2% になった。
   **天井に見えたのは情報源の天井であって、情報の天井ではなかった。**

   **残り1033の内訳。**

   | 項目 | 空欄 | 充足 | 項目 | 空欄 | 充足 |
   |---|---|---|---|---|---|
   | coldbath_season | 267 | 7% | sauna_type | 119 | 58% |
   | kitchen_type | 159 | 44% | coldbath | 95 | 67% |
   | stove | 129 | 55% | outdoor_rest | 88 | 69% |
   | loyly | 121 | 58% | pet_ok / wifi / sauna_exists | 55 | 90〜100% |

   **サウナイキタイ（255/255）も楽天（192/192）も使い切った。**
   残り1033のうち `coldbath_season` が26%を占めるが、これは**否定形の季節情報**で、
   公式が書くのは「冬季休止」の注意書きがある施設だけ。構造的に埋まらない。

   **効いた情報源には3つの条件が共通していた。**
   (1) 多数の施設を**同じ構造**で載せている、(2) 公式が書かない項目を持っている、
   (3) **掲載の有無を安く判定できる**。楽天は (3) がそもそも不要だった
   （DBが施設番号を持っているのでURLが決まる）。**次の候補を探すときはこの3条件で測る。**

   **残りの主要4項目（`kitchen_type` 159 / `stove` 129 / `loyly` 121 /
   `sauna_type` 119、合計528空欄）は、アグリゲーターでは埋まらないと確かめた。**
   2026-09 にじゃらんとなっぷを1施設ずつ開いて構造を見た（後述）。**どちらも
   火口の種類もサウナの熱源・形式も持っていない。4つ目を探すのは筋が悪い。**

   **アグリゲーターは「アメニティと施設種別」をモデル化しており、「設備の仕様」は
   持たない。** サウナイキタイだけが仕様をモデル化していたので効いたが、そこは
   使い切った。残る道は **(1) 公式サイトを1施設ずつ読む（実績2割の収量）、
   (2) オーナー調査**の2つ。
4. ~~**マイグレ15施設の `stove`**~~ — **2026-09 に机上で解決した。**
   サウナイキタイのサウナ室タグが熱源を持っており、掲載のあった14施設すべてが
   「電気」でブランド内で1件も割れていなかった。値は変えず出典を付けた。
   **`stove` の出典なしは 34件 → 3件。** 残るのは id=60 / id=74 / id=228。
   **オーナー調査を待たずに済んだ。** 同じ手が他の「オーナー調査待ち」項目にも効く
   （`sauna_temp` 26→73 / `sauna_cap` 46→86 / `water_temp` 11→50 /
   `rest_chair` 37→63 / `water_depth` 1→38）。
5. ~~**`ota` の壊れたリンク**~~ — **2026-09 に2度片付けた。** 1度目は Airbnb 5件を
   実機照合ができないため削除（候補URLを `data/desk-research.js` に残した。
   **実機で住所を照合できたら入れ直す**）。

   **2度目は楽天の全件走査で5件。** ページ上の住所とDBの `addr` を照合して見つけた。
   差し替え2件（id=155 は同ブランドの「ZUSHI Funny house」を指していた／
   id=278 は楽天トラベルキャンプに移っていた）、削除3件（id=239 は姉妹施設の
   「AMAO VILLA 熱海」で差し替え先が無い／id=83・id=285 は掲載終了と404）。
   **番号が隣接していても別施設**（id=6 CAP MARTIN が `188884`、id=155 の誤りが `188882`）。
6. **spec-survey.html の作成** — ステップ1（項目の凍結）は `early_late` の
   分割で解消済み。着手できる状態
7. **チャネルDの入力欄実装** — 母数が要るので着手が早いほど良い。
   ステップ2にしか依存しないのでオーナー送付を待たずに先行できる
8. **公式URLの訂正** — Jade Group 3件はステージング環境と別物件に誤誘導する。
   Avalon Cove の terracecollections.jp は「リンク集のみ」ではなく
   3施設を束ねたブランド公式サイトだったので分類を見直した

8b. ~~**URLのクエリとロケール**~~ — **2026-09 に完了。** 検索セッション・広告計測の
   クエリ356件を外し（残るクエリ付きは施設の特定に必要な3件だけ）、Agoda 65件を
   `ja-jp` に、Expedia 34件を `www.expedia.co.jp` の日本語ページに直した。
   `validate.py` に追跡クエリ検査と `BAD_LOCALE` 検査を入れて再発を止めた。

8c. ~~**個別ページの地図URL**~~ — **2026-09 に完了。** 住所は villas/*.html に
   4通りの形で出るが、地図リンクと地図埋込は percent-encode されており平文の置換が
   当たっていなかった。16施設32URLが旧住所を指したままで、**住所を訂正した施設ほど
   誤っていた。** `fix_villa.py` に符号化形の置換を追加し、既存分はDBから作り直し、
   `validate.py` に `check_map_urls` を入れた。
8d. ~~**primera villa（id=69）のサウナの再確認**~~ — **2026-09 に解決した。**
   一度「実在しない」と結論して値とタグを消したが、**その根拠は公式サイトの
   不記載だけ**で、不記載は否定の根拠にならないという自分たちの規約に反していた。
   一休 00052313 のFAQ「サウナはありますか？」→「**ございます。**」、客室紹介
   「**セルフロウリュウが楽しめるプライベートサウナ**」「**水風呂代わりのプールへ**」
   「**外気浴**は」から6項目を記録し `sauna` タグを戻した。
   **公式 primera-gr.co.jp はグループサイトではなく単独施設のサイトだった**
   （トップから定員12名・4LDK・FAQ が出る）。住所も一休「富津市金谷530-1」で一致。
   **`coldbath_season=winter` だけは推論を含む。** プールの利用可能期間が
   「2026/6/1~2026/9/30」で、実際は**夏季のみ**。選択肢では「10〜5月が休止」を
   表現できない（`coldbath_season` のスキーマ不足の3例目）
9. **id=239 AMAO VILLA の座標の取り直し** — addr を富戸911-122に訂正したが、
   `data/coordinates.json` は旧 addr（姉妹施設の住所）で引いた座標のままで、
   チャネルAの6項目が AMAO VILLA 伊豆高原のものになっている。
   `GMAPS_KEY` を設定して `tools/places_geocode.py` と `tools/channel_a.py` を
   再実行する必要がある。**feature と desc は 2026-09 に差し替え済み。**
10. **トライハク神楽（id=197）のドッグラン面積** — 2026-09 に調べたら
    **2つではなく3つの数字**が出た。公式本文「400㎡」／同じページの諸元表「250㎡」／
    予約エンジン「350㎡」（DBの紹介文と一致）。**同一運営の3つの掲載面がそれぞれ
    違う数字を出しており決められない。** DBの 350 は3つのうち最も運用に近い数字では
    あるので、紹介文はそのままにしてある。**サイト内の食い違いは「2つのうちどちらか」
    とは限らない。**
11. ~~**`kitchen_type` のカセットコンロ方針**~~ — **2026-09 に `cassette` を
    追加して解消。** id=95 の `gas` を `cassette` に訂正し、保留していた
    id=211 / id=24 / id=23 も記録した（現在5件）。**「備え付けが無い」が条件**で、
    「カセットコンロ、**３口コンロ**」と併記する id=17 と、レンタル扱いの id=266 は
    入れない。

12. **走査で立ったフラグ（未決着）**

    **住所の枝番が割れている5件**（国土地理院では番地までしか解決せず切り分けられない）

    | 施設 | DB | 楽天 |
    |---|---|---|
    | id=50 THE CLUB 919 | 不動堂452-**4** | 452-**22**（楽天名も別ブランド表記） |
    | id=167 箱根リゾートyamaki | 仙石原**474-30** | **480-1** |
    | id=228 マイグレパノラマ | 吉田888-**45** | 888-**44** |
    | id=250 プライベートリゾート南風 | 大川1075-**46** | 1075-**47** |
    | id=252 伊豆高原テントリゾート | 池614-**171** | 614-**168** |

    **id=252 は出典を1つ足しても割れ方が変わらなかった。** 楽天は公式と同じ 168、
    booking・なっぷが 171（DBもこちら）で**2対2のまま**。id=197 のドッグラン面積と
    同じ扱いで値は変えていない。

    **id=191 軽井沢HOUSE VILLA は棟違いとみて据え置いた。** DB「長倉5575-8」が
    保存座標から 0.03 km、楽天「長倉4588-38」が 0.33 km。6棟構成なので棟ごとに
    番地が違うとみるのが自然。

    **そのほか**

    - **id=275 の住所に第三の表記** — ikitai「常磐町1-2-**44**」。DBの 1-2-**45**
      （3出典で確認済み）とも、姉妹施設 清香庵の 1-2-**43** とも違う。
      **同じ地番に3表記が流通している。** DBは据え置き
    - **id=244 の `water_temp`** — 公式 `t1015` と一休「6℃」（`u10`）の食い違いで
      保留中。**サウナイキタイに期待したが温度欄が「-」で未入力**だった。保留継続
    - **id=215 マイグレテラスの `kitchen_type`** — 楽天の自由文が
      「**コンロ：IH 一口＋カセットコンロ**」で `ih` ＋ `kitchen_burners: 1` に
      できるが、**id=236 で自由文に別施設の内容が混ざった直後なので入れていない。**
      公式 maigre.jp/terrace は JS 描画で本文が取れず裏が取れなかった
    - **チェックイン時刻の食い違い29件** — 公式が終了時刻を書かず、一休を持たない
      施設は決着手段が無い（前掲）

---

## その他

- **口コミ機能**: Firebase Firestore（project: villafaras-reviews、Spark プラン）。
  security rules で `approved: false` を強制、公開読み取りは承認済みのみ。
  新着通知は Google Apps Script から `tomoro-sato@alfaras.jp` へ。
  **オーナー調査票（`specSubmissions`）は同じプロジェクトを使うが、ルールが未設定**（前述）。
- **ローカル確認**: `.claude/serve.py` が `http://127.0.0.1:8765` で配信する。
  `python3 -m http.server` は**サンドボックスで `os.getcwd()` が拒否されて起動しない**ので、
  chdir してから使うこのスクリプトを通す。**リポジトリの位置は `__file__` からたどる。**

  **`.claude/launch.json` は「起動する」設定にできない（2026-09）。** preview_start の
  ランチャは **Google ドライブ配下のファイルを開けない**。

  ```
  python3: can't open file '.claude/serve.py': [Errno 1] Operation not permitted
  ```

  **相対パスでも絶対パスでも同じ `Errno 1`**（`Errno 2` ではない）ので、パスの書き方の
  問題ではなく CloudStorage へのアクセス権。**Bash から起動すれば動く**ので、
  launch.json は `runtimeExecutable` を持たない**接続専用の設定**にしてある
  （`url` だけを書くと「起動せずに既存のサーバへ繋ぐ」）。手順は次の2つ。

  ```
  nohup python3 .claude/serve.py > /tmp/serve.log 2>&1 &     # 先に Bash で起動
  preview_start {name: "villafaras"}                          # それに接続する
  ```

  **ポートが埋まっていると preview_start は何もせずに止まる。** 前のセッションの
  サーバが残っていたら `lsof -ti:8765 | xargs kill` で落としてから起動し直す。

  **ブラウザのパネルが「hidden」のときは、別のパネルが手前にあるだけのことがある。**
  `get_layout` の `open_panes` を見て、`tasks` などが開いていたら閉じると出てくる。
- **リポジトリの置き場所（2026-09 に移動）**: `~/Desktop/villafaras` から
  **Google ドライブの共有ドライブ**（`~/Library/CloudStorage/GoogleDrive-…/共有ドライブ/Alfaras（株）/villafaras`）へ移した。
  移動で壊れたのは `.claude/serve.py` と `.claude/launch.json` の**絶対パスの決め打ちだけ**で、
  `tools/*.py` は全部リポジトリ直下からの相対パスなので無傷だった。
  **新しくツールを書くときも絶対パスを書かないこと。**

  共有ドライブに置いたことで増えた注意点。

  - **`.DS_Store` が追跡されていたので外した。** Finder が触るたびに差分に出る
  - **`.git` ごと同期される。** 別の端末やメンバーが同じフォルダを開いた状態で
    git 操作が重なるとリポジトリが壊れうる。**同時に触らないこと**
  - **旧 `~/Desktop/villafaras` が中身まで同一のまま残っている**（同じ HEAD・差分なし）。
    どちらを編集したか分からなくなるので、**確認のうえ片方を消す**
  - **git の操作が遅い。** `git status --ignored` や再帰 `diff` は2分で終わらない。
    調べるときは `find` や `ls` で範囲を絞る

### 共有アセットのキャッシュ（2026-09 に恒久対応）

`spec.js` / `spec.css` / `reviews.js` / `reviews.css` / `spec-data.js` は
index.html と villas/*.html から `<script src>` / `<link href>` で読まれる。
**素の URL のままだと、更新してもブラウザが古い版を使い続ける。**
HTML に `?v=` を付けても**リンク先には効かない**ので、開発中に
「直したのに反映されない」を何度も起こした（3列指定が落ちているのに
気づくのが遅れた）。**GitHub Pages でも同じで、再訪者に古い CSS/JS が配られる。**

対応は2つ。

1. **`tools/bust.py`** — 参照に内容ハッシュを付ける。**これらのファイルを
   編集したら必ず実行すること。**

   ```
   python3 tools/bust.py            差分を表示
   python3 tools/bust.py --write    書き込む
   ```

   `src="../spec.js"` → `src="../spec.js?v=25883bc4"`。中身が変わると
   ハッシュが変わるので、放っておいても新しい版が配られる。
2. **`.claude/serve.py` が `Cache-Control: no-store` を返す**（開発用のみ）。
   HTML 自体が古いまま出るのを止める。

**一度キャッシュに入った URL は、この対応の前後をまたぐと1回だけ手当てが要る。**
ブラウザのキャッシュは修正前のヘッダーで保持されているので、
`fetch(url, {cache:'reload'})` で取り直すか、スーパーリロードを1回する。

### 2026-09 の整理（83MB → 19MB）

- **`git gc` を一度も走らせていなかった。** ばらけたオブジェクトが
  **1,679個・42.71MiB**（パック済みは4.85MiB）。`gc` で `.git` が **48MB → 8.6MB**、
  ばらけた分は 44個・1.38MiB になった。**ドライブは小ファイルを個別に同期するので、
  容量よりも同期の負荷に効く。** 履歴は723コミットすべて無事
- **`.claude/worktrees/` に21MB（3つ×6.9MB）の孤児が残っていた。**
  `git worktree list` の登録は**旧 Desktop のパスを指したまま**移動してきており、
  `git worktree remove` は「does not point back to」で拒否する。
  **`.git/worktrees/` のメタデータを直接消してから `prune` する。**
  3本の `claude/*` ブランチは main に無いコミットを1件ずつ持っていたが、
  **内容（id=6 の住所261-5、id=275 の1-2-45、`check_map_urls`）はすべて main に
  別コミットとして入っている**ことを確認してから消した
- 古いバックアップ（`新ヴィラファラス/` 4.2MB＝2026-07-15 のスナップショット＋zip、
  `ヴィラ紹介サイト/` 184KB＝2026-07-09 の試作）を削除
- `out/` は `spec-data-desk.js`（マージの入力）だけ残し、
  チャネルAの中間生成物は削除。再実行で作り直せる
- **ルートの PDF 2件は残した。** `infomation.pdf`（1.0MB）は**タイトルが「宿泊約款」**で
  業務文書。`ver10_20160828.pdf`（2.7MB）は PowerPoint 書き出しで中身が不明。
  どちらもサイトからは参照されておらず `*.pdf` で git の管理外
- **Google Places API**: `places:searchText` で施設名＋住所からジオコーディング。
- **OSRM**: 所要時間の算出。**GSI（国土地理院）標高API**: 標高。
- Chrome 拡張機能は `claude --chrome` または `/chrome` で接続。
  ドメインごとに承認が必要。同一ドメイン内のサブページは追加承認不要。
  Wix など遅延読み込みのサイトはスクロールと待機が要る。
  重い JS はタブが固まるため `get_page_text` を優先する。
